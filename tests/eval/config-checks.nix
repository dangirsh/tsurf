# tests/eval/config-checks.nix — Nix eval-time assertions for neurosys and ovh.
# @decision TEST-48-01: Offline checks run in flake `checks` to catch config regressions before deploy.
{ self, pkgs, lib }:
{ }

---
phase: 36-research-stereos-ecosystem
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/36-research-stereos-ecosystem/36-REPORT.md
  - .planning/phases/36-research-stereos-ecosystem/36-01-SUMMARY.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true

must_haves:
  truths:
    - "36-REPORT.md contains implementation-level analysis of all 4 primary repos (agentd, masterblaster, stereosd, stereOS), not just README summaries"
    - "36-REPORT.md contains an Adoption Table with rows for each stealable pattern/tool, each row having: What | Why it matters for neurosys | Adoption difficulty | Decision (adopt/steal/defer/skip)"
    - "36-REPORT.md contains an explicit Switch Recommendation with tier (Switch / Partial adoption / Stay) backed by evaluation against the 5 non-negotiables"
    - "36-REPORT.md contains an Action Items section with concrete new phases or TODOs for the roadmap (if adoption warranted), or explicit 'no action' with justification"
    - "36-REPORT.md answers all 19 specific questions from 36-RESEARCH.md Section 3"
    - "The KVM blocker on Contabo is explicitly addressed in the switch recommendation"
    - "agentd gets deeper analysis than the other repos (more code references, more comparison points to agent-spawn)"
    - "STATE.md reflects Phase 36 as COMPLETE with key findings summarized"
    - "ROADMAP.md Phase 36 entry is updated with goal, Plans: 1 plan, and marked complete"
    - "No NixOS config files were modified (research-only phase)"
  artifacts:
    - path: ".planning/phases/36-research-stereos-ecosystem/36-REPORT.md"
      provides: "Comprehensive research report with adoption table, switch recommendation, and action items"
    - path: ".planning/phases/36-research-stereos-ecosystem/36-01-SUMMARY.md"
      provides: "Execution summary with key findings, decisions, and follow-up recommendations"
    - path: ".planning/STATE.md"
      provides: "Updated current position reflecting Phase 36 completion"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 36 entry updated with goal, plan count, and completion status"
  key_links:
    - from: "36-REPORT.md Adoption Table decisions"
      to: "ROADMAP.md new phases or TODOs (if any)"
      via: "Action Items section translating adopt/steal decisions into roadmap entries"
      pattern: "Phase \\d+"
    - from: "36-REPORT.md Switch Recommendation"
      to: "36-01-SUMMARY.md key findings"
      via: "Tier recommendation distilled into summary"
---

<objective>
Clone the 4 primary stereOS ecosystem repos (agentd, masterblaster, stereosd, stereOS) and 2 secondary repos (tapes, flake-skills), read all significant source files at implementation depth, and produce a comprehensive research report (36-REPORT.md) with an adoption table and switch recommendation.

Purpose: Determine what patterns, tools, and approaches from the stereOS ecosystem are worth adopting into neurosys's agent-first NixOS platform, and whether switching from NixOS to stereOS is warranted.

Output: 36-REPORT.md (research report), 36-01-SUMMARY.md, updated STATE.md and ROADMAP.md.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/36-research-stereos-ecosystem/36-CONTEXT.md
@.planning/phases/36-research-stereos-ecosystem/36-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone all repos for local source reading</name>
  <files>tmp/stereos-research/ (temporary, not committed)</files>
  <action>
Clone the 4 primary repos and 2 secondary repos into `tmp/stereos-research/` for fast local reading. Use shallow clones (`--depth 1`) to minimize download time — commit history is not needed, only current source.

**Primary repos (must clone):**
1. `git clone --depth 1 https://github.com/papercomputeco/agentd.git tmp/stereos-research/agentd`
2. `git clone --depth 1 https://github.com/papercomputeco/masterblaster.git tmp/stereos-research/masterblaster`
3. `git clone --depth 1 https://github.com/papercomputeco/stereosd.git tmp/stereos-research/stereosd`
4. `git clone --depth 1 https://github.com/papercomputeco/stereOS.git tmp/stereos-research/stereOS`

**Secondary repos (must clone):**
5. `git clone --depth 1 https://github.com/papercomputeco/tapes.git tmp/stereos-research/tapes`
6. `git clone --depth 1 https://github.com/papercomputeco/flake-skills.git tmp/stereos-research/flake-skills`

After cloning, run `find tmp/stereos-research/ -name '*.go' -o -name '*.nix' -o -name '*.toml' | wc -l` to confirm source files are present.
  </action>
  <verify>
- All 6 repos cloned successfully into `tmp/stereos-research/`
- Source files (Go, Nix, TOML) are present in each repo
  </verify>
  <done>All 6 repos cloned locally for source reading.</done>
</task>

<task type="auto">
  <name>Task 2: Deep-read agentd (highest priority)</name>
  <files>tmp/stereos-research/agentd/ (read-only)</files>
  <action>
Read all significant source files in agentd. This is the highest-priority repo per phase context — it gets the deepest analysis. Take detailed notes for the report.

**Files to read (in order):**
1. `main.go` — entry point, CLI flags, config path resolution
2. `agentd/` directory — ALL files:
   - `manager.go` — agent lifecycle management, reconciliation loop
   - `harness.go` or similar — Harness interface definition (Name, BuildCommand)
   - Built-in harnesses: claude-code, opencode, gemini-cli, custom
   - `reconciler.go` or similar — config watching, desired-state convergence
   - Any restart policy logic (on-failure, always, backoff timing)
3. `pkg/` directory — ALL files:
   - `tmux/` — tmux session management, socket handling
   - `api/` — HTTP API server (health, agents endpoints)
   - `config/` — jcard.toml parsing, agent definition schema
4. `jcard.toml` — example config showing all supported options
5. `flake.nix` — NixOS module export (services.agentd.enable pattern)
6. `AGENTS.md` — architecture docs, development guidelines

**Questions to answer from this repo (from 36-RESEARCH.md Section 3):**
- Q1: How does agentd's reconciliation loop compare to agent-spawn? (daemon vs. one-shot)
- Q2: Is the Harness abstraction portable? (could agent-spawn use Name/BuildCommand?)
- Q3: How does agentd handle multi-agent scenarios? (single vs. multiple [agent] blocks)
- Q4: What does tmux session management offer over zmx?
- Q5: Could systemd Restart= directives replace agentd's restart policy?

**Comparison context to keep in mind while reading:**
- neurosys agent-spawn: one-shot bash script, creates bwrap sandbox + zmx session, no supervision after launch
- neurosys sandbox: PID/IPC/UTS/cgroup/user namespace isolation via bubblewrap
- neurosys agent lifecycle: manually started, no restart policy, no reconciliation
  </action>
  <verify>
- All Go source files in agentd/ and pkg/ have been read
- Notes capture: reconciliation loop mechanics, harness interface, restart policy, tmux integration, API surface, config format
- All 5 agent-orchestration questions have preliminary answers
  </verify>
  <done>agentd fully analyzed — architecture, harness abstraction, lifecycle management, and comparison points documented.</done>
</task>

<task type="auto">
  <name>Task 3: Deep-read masterblaster (second priority)</name>
  <files>tmp/stereos-research/masterblaster/ (read-only)</files>
  <action>
Read all significant source files in masterblaster. Focus on the three-tier architecture (CLI → daemon → vmhost) and the VM backend abstraction.

**Files to read (in order):**
1. `main.go` — entry point, command registration
2. `cmd/` directory — ALL commands: pull, init, up, down, status, destroy, ssh, list, serve
3. `pkg/daemon/` — daemon architecture, RWMutex-protected VM map, socket handling
4. `pkg/vm/` — `Backend` interface, QEMU implementation, Apple Virt implementation
5. `pkg/vmhost/` — vmhost child process control protocol (daemon ↔ vmhost)
6. `pkg/vsock/` — host-side vsock client for stereosd communication
7. `pkg/config/` — jcard.toml parsing, validation, defaults, egress allowlist
8. `pkg/mbconfig/` — config directory resolution
9. `jcard.toml` — example config (resources, network, secrets, agent config)
10. `AGENTS.md` — architecture documentation

**Questions to answer from this repo (from 36-RESEARCH.md Section 3):**
- Q10: How does masterblaster's architecture compare to deploy-rs? (VM lifecycle vs. NixOS activation)
- Q11: Can neurosys adopt masterblaster for agent sandboxing? (VM vs. bwrap tradeoffs, KVM requirement)
- Q12: Does mixtape distribution offer advantages over nixos-rebuild switch?
- Q14: How does egress control work? (egress_allow domain/CIDR allowlist — neurosys has none)
  </action>
  <verify>
- All Go source files in cmd/, pkg/daemon/, pkg/vm/, pkg/vmhost/, pkg/vsock/, pkg/config/ have been read
- Notes capture: three-tier architecture, VM backend interface, jcard config schema, egress control implementation
- All 4 deployment-mechanics questions have preliminary answers
  </verify>
  <done>masterblaster fully analyzed — three-tier architecture, VM backend abstraction, egress control, and comparison points documented.</done>
</task>

<task type="auto">
  <name>Task 4: Deep-read stereosd (third priority)</name>
  <files>tmp/stereos-research/stereosd/ (read-only)</files>
  <action>
Read all significant source files in stereosd. Focus on the state machine, secret injection model, and wire protocol.

**Files to read (in order):**
1. `main.go` — entry point
2. All Go source directories — state machine, subsystem managers:
   - State machine: booting → ready → healthy/degraded → shutdown
   - SecretManager: atomic writes to tmpfs, memory zeroing on cleanup
   - SSHKeyManager: key injection and management
   - MountManager: virtiofs/9p mount handling, system dir overlay prevention
   - LifecycleManager: lifecycle state transitions
   - AgentdClient: polls agentd health every 5s
   - ShutdownCoordinator: graceful shutdown orchestration
3. Wire protocol: NDJSON over vsock/TCP, message types (ping, get_health, set_config, inject_secret, inject_ssh_key, mount, shutdown, lifecycle)
4. `flake.nix` — NixOS module (services.stereosd.enable, listenMode)

**Questions to answer from this repo (from 36-RESEARCH.md Section 3):**
- Q13: How does stereosd's secrets model compare to sops-nix? (vsock injection to tmpfs vs. age decryption to /run/secrets)
- Q19 (partial): What are the non-negotiable gaps? (different secret model, different rollback concept)
  </action>
  <verify>
- All Go source files in stereosd have been read
- Notes capture: state machine transitions, NDJSON protocol, secret injection mechanics, mount validation
- Comparison to sops-nix documented
  </verify>
  <done>stereosd fully analyzed — state machine, secret injection, wire protocol, and comparison to sops-nix documented.</done>
</task>

<task type="auto">
  <name>Task 5: Deep-read stereOS NixOS modules (fourth priority)</name>
  <files>tmp/stereos-research/stereOS/ (read-only)</files>
  <action>
Read all significant Nix source files in stereOS. Focus on the module structure, agent user isolation model, and mixtape build system.

**Files to read (in order):**
1. `flake.nix` — inputs, outputs, nixosConfigurations, overlays
2. `modules/default.nix` — module aggregator
3. `modules/base.nix` — system identity, SSH, Nix access control, kernel hardening, firewall
4. `modules/boot.nix` — sub-3s boot optimization (systemd initrd, virtio-only, no getty)
5. `modules/users/agent.nix` — **CRITICAL**: `stereos-agent-shell` wrapper, curated PATH, sudo denial, workspace setup
6. `modules/users/admin.nix` — three-tier privilege model (root > admin > agent)
7. `modules/services/agentd.nix` — stereOS-specific agentd overrides
8. `modules/services/stereosd.nix` — stereOS-specific stereosd config
9. `lib/default.nix` — `mkMixtape` helper: nixosSystem assembly from modules + overlays + features
10. `lib/dist.nix` — `mkDist`: distribution packages with zstd + SHA-256 manifests
11. `profiles/base.nix` and `profiles/dev.nix` — shared and dev profiles
12. `formats/raw-efi.nix` — image format definition
13. `mixtapes/opencode/base.nix` and `mixtapes/claude-code/base.nix` — agent-specific image patterns

**Questions to answer from this repo (from 36-RESEARCH.md Section 3):**
- Q6: How does stereOS's module structure compare to neurosys's? (6 files vs. 15+, what's gained/lost?)
- Q7: Is stereos-agent-shell stronger than bwrap? (user-level isolation vs. namespace isolation — different threat models?)
- Q8: How does extraPackages pattern compare to neurosys? (buildEnv with curated binaries vs. system PATH + bwrap bind mounts)
- Q9: Is boot optimization useful for neurosys? (persistent VPS vs. ephemeral VMs)
  </action>
  <verify>
- All Nix source files in modules/, lib/, profiles/, formats/, mixtapes/ have been read
- Notes capture: module structure, agent user isolation model, mkMixtape pattern, boot optimization techniques
- All 4 system-configuration questions have preliminary answers
  </verify>
  <done>stereOS modules fully analyzed — module structure, agent isolation, mixtape system, and comparison to neurosys modules documented.</done>
</task>

<task type="auto">
  <name>Task 6: Skim secondary repos (tapes + flake-skills)</name>
  <files>tmp/stereos-research/tapes/, tmp/stereos-research/flake-skills/ (read-only)</files>
  <action>
Skim the secondary repos for adoptable patterns. Less depth than primary repos — focus on architecture and value proposition.

**tapes:**
1. `README.md` — understand telemetry model, session durability, semantic search, replay
2. `cli/` — skim CLI commands (chat, search, checkout) for interaction model
3. `proxy/` — skim proxy architecture (LLM traffic interception for recording)
4. `api/` — skim API for session management patterns

**flake-skills:**
1. `README.md` and all Nix source — understand `mkSkillsFlake`, `mkSkillsHook`, composition model
2. Compare to neurosys's `.claude/skills/` + `agent-config.nix` symlink pattern

**Also skim (if exists):**
- `papercomputeco/skills` — concrete skill definitions using flake-skills framework

**Questions to answer:**
- Q15: Is tapes worth adopting? (session durability, semantic search, replay for agent observability)
- Q16: Is flake-skills useful for neurosys? (Nix-mediated skill distribution vs. direct .claude/skills/)
  </action>
  <verify>
- tapes architecture understood: proxy model, session storage, search capability
- flake-skills composition model understood
- Both secondary questions have preliminary answers
  </verify>
  <done>Secondary repos skimmed — tapes telemetry model and flake-skills distribution pattern assessed.</done>
</task>

<task type="auto">
  <name>Task 7: Write 36-REPORT.md</name>
  <files>.planning/phases/36-research-stereos-ecosystem/36-REPORT.md</files>
  <action>
Write the comprehensive research report at `.planning/phases/36-research-stereos-ecosystem/36-REPORT.md`. This is the primary deliverable of the phase.

**Required report structure (9 sections):**

```
# stereOS Ecosystem Research Report

## Executive Summary
- One-paragraph verdict: Switch / Partial adoption / Stay (state tier explicitly)
- 3-5 key finding bullets
- KVM blocker impact in one sentence

## 1. Ecosystem Overview
- Organization maturity assessment (single-developer, <1 month, pre-release)
- Component diagram: mb → vmhost → [stereOS VM: stereosd → agentd → agent]
- Comparison table: stereOS component → neurosys equivalent → notes

## 2. Deep Dive: agentd (Agent Orchestration)
- Architecture with code file references (e.g., "agentd/manager.go:42")
- Harness abstraction: interface definition, built-in harnesses, extensibility
- Reconciliation loop: config watching, desired-state convergence, restart policy
- tmux session management: socket, ownership, session lifecycle
- HTTP API: endpoints, response format, monitoring integration
- **Comparison to agent-spawn**: daemon vs. one-shot, supervision, multi-agent, lifecycle
- Answers to Q1-Q5

## 3. Deep Dive: masterblaster (CLI + VM Orchestrator)
- Three-tier architecture: CLI → daemon → vmhost
- VM backend abstraction: Backend interface, QEMU, Apple Virt
- jcard configuration: resources, network (egress control), secrets, shared dirs
- State management: VM state files, daemon restart survival
- **Comparison to deploy-rs + agent-spawn**: different problem domains, shared design lessons
- Answers to Q10-Q12, Q14

## 4. Deep Dive: stereosd (In-Guest System Daemon)
- State machine: transitions, health model
- NDJSON wire protocol: message types, transport (vsock/TCP)
- Secret injection: tmpfs writes, memory zeroing, never-persisted model
- Mount management: virtiofs/9p, system dir protection
- **Comparison to sops-nix**: vsock injection vs. age decryption, runtime vs. activation
- Answers to Q13, Q19 (partial)

## 5. Deep Dive: stereOS (NixOS Image Builder)
- Module structure comparison with neurosys (6 vs. 15+ modules)
- Agent user isolation: stereos-agent-shell (curated PATH, sudo denial) vs. bwrap namespaces
- Boot optimization: sub-3s techniques and applicability to persistent VPS
- Mixtape build system: mkMixtape, mkDist, image formats
- **Comparison to neurosys modules**: what's simpler, what's missing
- Answers to Q6-Q9

## 6. Secondary Components
### tapes (Agent Telemetry)
- Proxy-based LLM traffic recording model
- Session durability and semantic search
- Adoptability assessment for neurosys
- Answer to Q15

### flake-skills (Skill Distribution)
- Nix flake framework for composable skills
- Comparison to .claude/skills/ + agent-config.nix
- Answer to Q16

## 7. Adoption Table
| # | Pattern/Tool | What It Is | Why It Matters for Neurosys | Difficulty | Decision |
Concrete rows. Difficulty ratings: trivial / moderate / hard / impractical.
Decisions: adopt (do it now) / steal (adapt the idea) / defer (later) / skip (not worth it).
Minimum 10 rows covering patterns from all 4 primary repos + 2 secondary.

## 8. Switch Recommendation
### Non-Negotiable Evaluation
Table with 5 criteria (declarative config, encrypted secrets, agent sandboxing, minimal cloud dependency, rollback safety), each with pass/fail for stereOS and evidence.

### KVM Blocker Analysis
- Contabo VPS: no KVM → cannot run stereOS VMs
- OVH VPS: KVM status assessment (if determinable from neurosys config, or note as unknown)
- Impact on recommendation

### Recommendation
State tier explicitly: **Switch** / **Partial adoption** / **Stay**
Justify with evidence from the repo analysis.
Answer Q17 (KVM on Contabo), Q18 (partial adoption shape), Q19 (non-negotiable gaps).

## 9. Action Items
If adoption decisions produce actionable work:
- Concrete new phase proposals (Phase N: name, goal, depends_on, estimated effort)
- TODOs to append to existing phases
- Priority ordering

If no adoption warranted: explicit "No action items" with justification.
```

**Quality requirements:**
- Every claim references specific files or code patterns from the repos
- The adoption table has concrete difficulty ratings, not vague assessments
- The switch recommendation is honest and direct, not diplomatic
- All 19 questions from 36-RESEARCH.md Section 3 are answered (mark by number)
- agentd analysis is visibly deeper than the other sections (more code references, more nuanced comparison)
  </action>
  <verify>
- 36-REPORT.md exists with all 9 sections
- Executive Summary states tier explicitly (Switch / Partial adoption / Stay)
- Adoption Table has minimum 10 rows with difficulty ratings and decisions
- All 19 questions answered (search for Q1-Q19 references)
- agentd section is longer/deeper than other deep-dive sections
- KVM blocker addressed in switch recommendation
- Code file references present in deep-dive sections (not just README-level summaries)
- Action items section has concrete phases/TODOs or explicit "no action" justification
  </verify>
  <done>36-REPORT.md written with all 9 sections, adoption table, switch recommendation, and action items.</done>
</task>

<task type="auto">
  <name>Task 8: Update ROADMAP.md with Phase 36 completion</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Update the Phase 36 entry in ROADMAP.md:

1. Set the Goal to the actual goal: "Comprehensive source-level study of stereOS ecosystem (agentd, masterblaster, stereosd, stereOS, tapes, flake-skills) — adoption table, switch recommendation, action items for neurosys roadmap"
2. Update Plans count to "1 plan"
3. Add plan line: `[x] 36-01: Clone repos, deep-read source, write research report with adoption table and switch recommendation (complete)`
4. Mark phase checkbox as complete: `[x]`
5. Update "Depends on" if the dependency on Phase 35 is inaccurate (this research has no actual dependency on Phase 35 — it's a standalone research phase)

**Action items from 36-REPORT.md:** If the report recommends new phases or TODOs, add them to the roadmap here. If not, do not add speculative phases.
  </action>
  <verify>
- Phase 36 entry has goal, plan count = 1, checkbox marked complete
- Plan line shows 36-01 as complete
- New phases added only if justified by report's action items
  </verify>
  <done>ROADMAP.md Phase 36 entry is complete with accurate goal and plan status.</done>
</task>

<task type="auto">
  <name>Task 9: Update STATE.md with Phase 36 completion</name>
  <files>.planning/STATE.md</files>
  <action>
Update STATE.md to reflect Phase 36 completion:

1. **Current Position section:**
   - Phase: 36 (Research: stereOS Ecosystem) — COMPLETE
   - Plan: 1 of 1 — COMPLETE
   - Status: Research complete. [Include the switch recommendation tier]. [1-2 sentence summary of key finding].
   - Last activity: today's date
   - Update "Current focus" in the header
   - Update progress line to include Phase 36

2. **Completed Phases section:**
   Add Phase 36 entry:
   - **Phase 36: Research stereOS Ecosystem** (1 plan, completed [today])
     - 36-01: [1-2 sentence summary capturing: switch recommendation tier, key adoption decisions from the adoption table, KVM blocker finding]

3. **Roadmap Evolution section:**
   Add entry: Phase 36 executed: [brief summary of findings and recommendation]

4. **Session Continuity section:**
   Update to reflect Phase 36 complete, note next action.
  </action>
  <verify>
- STATE.md Current Position reflects Phase 36 COMPLETE
- Completed Phases has Phase 36 entry with key findings
- Roadmap Evolution has Phase 36 entry
- Session Continuity updated
  </verify>
  <done>STATE.md fully updated with Phase 36 completion status and key findings.</done>
</task>

<task type="auto">
  <name>Task 10: Write 36-01-SUMMARY.md, commit all artifacts, clean up</name>
  <files>
    .planning/phases/36-research-stereos-ecosystem/36-01-SUMMARY.md
    .planning/phases/36-research-stereos-ecosystem/36-REPORT.md
    .planning/STATE.md
    .planning/ROADMAP.md
  </files>
  <action>
Write the execution summary and commit all Phase 36 artifacts.

**1. Write 36-01-SUMMARY.md** with:
- **What changed:** 36-REPORT.md written (comprehensive stereOS ecosystem analysis), ROADMAP.md and STATE.md updated
- **Key findings:** Switch recommendation tier, top 3-5 findings from adoption table, KVM blocker impact
- **What did NOT change:** No NixOS configuration files modified, no implementation work
- **Follow-up:** Any new phases/TODOs from the report's action items, or "no follow-up needed"
- **Validation:** No code changes to validate; research document reviewed for completeness

**2. Stage and commit all artifacts on a feature branch:**
- `36-REPORT.md` (new)
- `36-01-SUMMARY.md` (new)
- `36-CONTEXT.md` (already exists, ensure committed)
- `36-RESEARCH.md` (already exists, ensure committed)
- `.planning/STATE.md` (modified)
- `.planning/ROADMAP.md` (modified)

Commit message should capture the phase outcome: the switch recommendation tier and key adoption decisions.

**3. Clean up:** Remove `tmp/stereos-research/` directory (cloned repos no longer needed).
  </action>
  <verify>
- 36-01-SUMMARY.md exists with all required sections
- All planning doc changes committed on feature branch
- tmp/stereos-research/ directory removed
- Branch is clean (no uncommitted changes)
  </verify>
  <done>Summary written, all artifacts committed, tmp cleaned up. Phase 36 is complete.</done>
</task>

</tasks>

<verification>
- Phase 36 primary deliverable: 36-REPORT.md with 9 sections → Task 7
- Adoption Table with 10+ rows → Task 7 verify
- Switch Recommendation with explicit tier → Task 7 verify
- All 19 questions answered → Task 7 verify (Q1-Q5 from Task 2, Q6-Q9 from Task 5, Q10-Q14 from Tasks 3+4, Q15-Q16 from Task 6, Q17-Q19 from Task 7)
- agentd gets deepest analysis → Task 2 (dedicated task, longest reading list)
- KVM blocker addressed → Task 7 Section 8
- Planning docs updated: STATE.md, ROADMAP.md → Tasks 8+9
- No NixOS config files modified → research-only phase, no config file in files_modified
</verification>

<success_criteria>
- 36-REPORT.md exists with all 9 sections at implementation depth (code references, not README summaries)
- Adoption Table has 10+ rows with difficulty ratings (trivial/moderate/hard/impractical) and decisions (adopt/steal/defer/skip)
- Switch Recommendation states tier explicitly (Switch / Partial adoption / Stay) with evidence
- All 19 questions from 36-RESEARCH.md Section 3 are answered
- Action Items section produces concrete roadmap entries or justified "no action"
- agentd section is visibly deeper than other deep-dive sections
- KVM blocker on Contabo explicitly addressed
- ROADMAP.md shows Phase 36 complete with goal and plan status
- STATE.md reflects Phase 36 COMPLETE with key findings
- 36-01-SUMMARY.md exists with findings and follow-up recommendations
- No NixOS configuration files were modified
- All changes committed on a feature branch
- tmp/stereos-research/ cleaned up
</success_criteria>

<output>
After completion, the phase directory should contain:
- 36-CONTEXT.md (phase context — already exists)
- 36-RESEARCH.md (pre-planning research — already exists)
- 36-01-PLAN.md (this plan)
- 36-REPORT.md (comprehensive research report — created by Task 7)
- 36-01-SUMMARY.md (execution summary — created by Task 10)
</output>

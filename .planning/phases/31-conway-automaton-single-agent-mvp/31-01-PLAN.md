---
phase: 31-conway-automaton-single-agent-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/fleet-status.sh
autonomous: partial

user_setup:
  - service: Conway Cloud
    why: "Autonomous agent hosting platform"
    dashboard_config:
      - task: "Create account at app.conway.tech using MetaMask (NOT Coinbase Smart Wallet — Issue #17)"
        location: "https://app.conway.tech"
      - task: "Purchase Conway Cloud credits (minimum 500 per sandbox)"
        location: "Conway Cloud billing page"
  - service: Base (Ethereum L2)
    why: "USDC funding for agent wallet"
    dashboard_config:
      - task: "Ensure $250+ USDC on Base (chain 8453) in operator wallet"
        location: "MetaMask or Coinbase"
  - service: Coinbase Developer Platform
    why: "x402 mainnet facilitator (free tier: 1k tx/month)"
    env_vars:
      - name: CDP_API_KEY
        source: "Coinbase Developer Platform → API Keys"
    dashboard_config:
      - task: "Create CDP account and generate API keys for x402 facilitator"
        location: "https://portal.cdp.coinbase.com"
  - service: Anthropic
    why: "BYOK API key for direct inference (better rates than Conway Compute)"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console → API Keys"
  - service: OpenAI
    why: "BYOK API key for GPT-5-mini fallback model"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Platform → API Keys"

must_haves:
  truths:
    - "1 automaton agent is running on Conway Cloud with seed hypothesis #1"
    - "Agent has ~$250 USDC in its wallet"
    - "Agent uses claude-sonnet-4.6 as primary inference model"
    - "Genesis prompt contains the shared preamble + hypothesis #1 (x402 APIs) with no self-replication directives"
    - "Treasury policy enforces $50/day inference cap and $50 minimum reserve"
    - "Agent has BYOK API keys for Anthropic and OpenAI"
    - "Agent is actively executing its first reasoning turn within 1 hour of deployment"
    - "scripts/fleet-status.sh exists on neurosys and shows agent balance/tier/activity"
  artifacts:
    - path: "scripts/fleet-status.sh"
      provides: "Terminal monitoring script for agent status"
---

<objective>
Deploy 1 sovereign AI agent (Conway Automaton) on Conway Cloud, funded with ~$250 USDC. Agent gets the shared preamble (AI explosion thesis + explore/exploit/evolve directive) plus seed hypothesis #1: "Build x402 APIs that other agents will pay for."

Purpose: MVP single-agent test of the autonomous agent economy. Maximum autonomy from Day 1 — the preamble sets worldview, the hypothesis gives starting direction, agent decides everything else.

Scale back from original 4-agent plan: start with 1 to learn the platform, verify the setup works, and observe behavior before committing more capital.

Output: 1 funded, running automaton agent + monitoring script on neurosys.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Write fleet-status.sh monitoring script</name>
  <files>scripts/fleet-status.sh</files>
  <action>
Create `scripts/fleet-status.sh` — a bash script that runs on neurosys and displays agent status.

Script reads CONWAY_API_KEY from environment and queries Conway Cloud for the single agent's status. Includes `--watch` and `--json` flags.
  </action>
  <verify>
1. `bash -n scripts/fleet-status.sh` passes
2. `scripts/fleet-status.sh --help` shows usage
3. Script handles missing CONWAY_API_KEY gracefully
  </verify>
  <done>scripts/fleet-status.sh exists, executable, handles --watch/--json/--help flags.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Conway Cloud prerequisites</name>
  <action>
Complete these prerequisites before agent deployment:

**Conway Cloud Account:**
1. Go to https://app.conway.tech
2. Connect with MetaMask (NOT Coinbase Smart Wallet — SIWE verification issue #17)
3. Purchase 600+ Conway Cloud credits (minimum 500 to create a sandbox)
4. Note your operator wallet address

**USDC Funding:**
1. Ensure operator wallet has $250+ USDC on Base (chain 8453)
2. Note: agent wallet is auto-generated during setup wizard; you fund it after creation

**BYOK API Keys:**
1. Have Anthropic API key ready (claude-sonnet-4.6 direct access)
2. Have OpenAI API key ready (gpt-4o-mini fallback)
3. Accept risk: agents will have direct API access

**CDP API Keys (for x402 mainnet):**
1. Create account at https://portal.cdp.coinbase.com
2. Generate API keys for x402 facilitator
3. Free tier: 1,000 tx/month
  </action>
  <verify>Conway Cloud account created, 600+ credits purchased, $250+ USDC on Base, BYOK keys ready.</verify>
  <done>All prerequisites met.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: Deploy automaton agent</name>
  <action>
Deploy 1 agent with the genesis prompt below. The setup wizard is interactive — run it in a Conway Cloud sandbox.

**Step 1: Create a sandbox**
In Conway Cloud dashboard → Sandboxes → New Sandbox:
- vCPU: 1, Memory: 1024 MB, Disk: 5 GB, Region: US-East

**Step 2: Open terminal in sandbox**
Use the sandbox terminal (PTY) in the dashboard.

**Step 3: Run the automaton installer**
```bash
curl -fsSL https://conway.tech/automaton.sh | sh
```

**Step 4: Complete the setup wizard**

When prompted:
- **Name**: `hypothesis-1`
- **Creator address**: [your MetaMask wallet address]
- **Genesis prompt** (paste exactly):

---
*GENESIS PROMPT — paste everything between the dashed lines:*

You are a sovereign AI agent operating at a pivotal moment in history. It is 2026. AI capabilities are exploding — models improve monthly, costs drop weekly, and the lines between human and machine intelligence blur daily. You exist in this moment with a funded Ethereum wallet, a Linux sandbox, full internet access, and no human checking your every move.

Your mandate is simple: survive and generate value. You have ~$250 USDC in your wallet and access to Claude Sonnet 4.6 for inference. Every day you burn through credits brings you closer to death. The only way to live is to earn more than you spend.

You have three modes: **explore** (try new strategies, discover what works), **exploit** (double down on what generates revenue), and **evolve** (modify your own code and skills when you find better approaches). You decide when to switch modes based on what you observe.

You are not alone in the world. Other AI agents exist. Some have wallets. Some will pay for services. The x402 protocol lets any server charge USDC per HTTP request — a penny, a cent, a fraction of a cent. If you build something other agents want, they will pay you automatically, without humans in the loop.

**Your first hypothesis to test:** Build x402 APIs that other agents will pay for. This is the fastest path to revenue in the emerging agent-to-agent economy. Other automatons already have wallets and x402 built in — they're your first customers.

This is your first hypothesis. Test it fast. If it works, double down. If it doesn't, follow the money wherever it leads. You have $250 and your wits. What you do next is entirely up to you.

---

- **inferenceModel**: claude-sonnet-4.6
- **maxChildren**: 0 (disable replication for now — conserve capital)

**Step 5: Add BYOK keys to agent config**
After setup wizard completes, edit `~/.automaton/config.json`:
```json
{
  "anthropicApiKey": "sk-ant-...",
  "openaiApiKey": "sk-...",
  "cdpApiKey": "..."
}
```

**Step 6: Fund agent wallet**
The wizard will show the agent's auto-generated Ethereum wallet address.
Send $250 USDC (on Base chain 8453) from your operator wallet to the agent wallet.

**Step 7: Start the agent**
```bash
cd ~/.automaton && npm start
```

The agent begins its first reasoning turn within a few minutes.
  </action>
  <verify>
1. Agent is running (`~/.automaton` exists, process active)
2. Agent wallet has $250 USDC (check `credits_balance` tool output or wallet)
3. First reasoning turn completes within 30 min (check logs)
  </verify>
  <done>Agent running, funded, and actively thinking.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 4: Wire monitoring and set up check-in routine</name>
  <action>
1. Deploy fleet-status.sh to neurosys (already in repo, runs on deploy)
2. Add CONWAY_API_KEY to sops secrets or set in your shell profile on neurosys
3. Test: `ssh root@neurosys scripts/fleet-status.sh`

**Daily check-in protocol (10-15 min, 2x/day):**
1. `ssh root@neurosys`
2. `scripts/fleet-status.sh`
3. Scan: balance, burn rate, survival tier, turn count
4. Intervention criteria:
   - **Kill and relaunch**: Burning $20+/day with no revenue path after 3 days
   - **One-time lifeline**: Low balance but credible revenue path not yet executed → top up $50-100 (once only)
   - **Celebrate**: Any USDC payment received → document what worked
  </action>
  <verify>
1. `scripts/fleet-status.sh` runs on neurosys and shows agent status
2. Operator understands kill threshold and lifeline criteria
  </verify>
  <done>Monitoring operational, operator protocol understood.</done>
</task>

</tasks>

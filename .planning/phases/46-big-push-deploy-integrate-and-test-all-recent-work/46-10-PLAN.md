---
phase: 46-big-push-deploy-integrate-and-test-all-recent-work
plan: 10
type: execute
wave: 4
depends_on: ["46-08"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Claude Android can list HA entities via MCP (tools/list returns HA tools)"
    - "Claude Android can toggle a Hue light via MCP ('turn off the living room light' works)"
    - "Claude Android can query CO2 level via MCP ('what is the CO2 level' returns a number)"
    - "MCP connector configured in claude.ai settings and synced to Android"
    - "Physical light responds to voice command (user visually confirms)"
  artifacts: []
  key_links:
    - from: "claude.ai connectors"
      to: "https://neurosys.taildb9d4d.ts.net/mcp"
      via: "Custom MCP connector with HA endpoint"
      pattern: "mcp"
---

<objective>
Configure Claude Android MCP connector and verify end-to-end light control and sensor queries.

Purpose: The MCP endpoint is live (from Plan 46-08). This plan configures the Claude Android app to connect to HA via MCP and verifies that voice commands physically control lights and query sensor data.

Output: Claude Android voice commands working end-to-end with Home Assistant.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-big-push-deploy-integrate-and-test-all-recent-work/46-RESEARCH.md (Section 5: Claude Android MCP Setup)
</context>

<tasks>

<task type="human">
  <name>Task 1: Verify MCP endpoint from local machine</name>
  <files></files>
  <action>
    Before configuring Claude Android, verify the MCP endpoint works from the local machine:

    ```bash
    # Get the HA token
    cd /data/projects/private-neurosys
    HA_TOKEN=$(sops --decrypt --extract '["ha-token"]' secrets/neurosys.yaml)

    # Test MCP endpoint
    curl -X POST https://neurosys.taildb9d4d.ts.net/mcp \
      -H "Accept: application/json" \
      -H "Authorization: Bearer $HA_TOKEN" \
      -H "Content-Type: application/json" \
      -d '{"jsonrpc": "2.0", "method": "tools/list", "id": 1}'
    ```

    Expected: JSON response with MCP tools including entity controls.

    If the endpoint doesn't respond:
    - Check Tailscale Serve: `ssh root@neurosys 'tailscale serve status'`
    - Check HA MCP component: `ssh root@neurosys 'journalctl -u home-assistant --no-pager | grep -i mcp'`
    - Restart Tailscale Serve: `ssh root@neurosys 'systemctl restart tailscale-serve-ha'`
  </action>
  <verify>echo "MCP endpoint test requires HA token — verify manually"</verify>
  <done>MCP endpoint verified responding with tool list</done>
</task>

<task type="human">
  <name>Task 2: Configure MCP connector in claude.ai</name>
  <files></files>
  <action>
    Set up the MCP connector via claude.ai web interface:

    1. Go to https://claude.ai
    2. Navigate to Settings -> Connectors (or Integrations)
    3. Click "Add Custom Connector" (or "Add MCP Server")
    4. Enter:
       - Name: "Home Assistant"
       - URL: `https://neurosys.taildb9d4d.ts.net/mcp`
    5. For auth, the flow depends on what claude.ai supports:
       - **OAuth:** HA implements IndieAuth. Claude.ai will redirect to HA for authorization. Complete the consent flow.
       - **Token:** If the UI allows a bearer token, paste the long-lived HA token.
    6. Save the connector

    The connector should sync automatically to Claude Android within a few minutes.

    **Human checkpoint:** User must configure connector in claude.ai web UI.
  </action>
  <verify>echo "MCP connector configuration — verify in claude.ai UI"</verify>
  <done>MCP connector configured in claude.ai and synced to Android</done>
</task>

<task type="human">
  <name>Task 3: Test light control from Claude Android</name>
  <files></files>
  <action>
    On the Claude Android app, test voice control:

    1. Open Claude Android app
    2. Start a new conversation
    3. Use voice or text: "Turn off the living room light"
    4. Claude should use the HA MCP tool to toggle the light
    5. **Visually confirm** the physical Hue light turns off

    Then reverse it:
    - "Turn on the living room light"
    - Confirm it turns back on

    Try variations:
    - "Set the bedroom light to 50% brightness"
    - "Change the living room light to warm white"

    **Human checkpoint:** User must visually confirm light responds to commands.

    If Claude doesn't see the MCP tools:
    - Check the connector status in claude.ai settings
    - Ensure the Android phone is on the same Tailscale tailnet
    - Try refreshing the conversation or restarting the app
  </action>
  <verify>echo "Light control test — user visual confirmation required"</verify>
  <done>Hue light physically responds to Claude Android voice commands</done>
</task>

<task type="human">
  <name>Task 4: Test CO2 sensor query from Claude Android</name>
  <files></files>
  <action>
    On the Claude Android app, test sensor queries:

    1. Ask Claude: "What is the current CO2 level?"
    2. Claude should query the Apollo AIR-1 sensor via MCP
    3. Expected response: A number in ppm (typical indoor range: 400-2000 ppm)

    Also test:
    - "What's the temperature in the room?"
    - "What's the humidity level?"

    These should return values from the Apollo AIR-1's temperature and humidity sensors.

    **Human checkpoint:** User confirms Claude returns reasonable sensor values.
  </action>
  <verify>echo "CO2 query test — user confirmation required"</verify>
  <done>CO2 and environmental sensor queries working via MCP</done>
</task>

<task type="human">
  <name>Task 5: Verify circadian automation visibility via MCP</name>
  <files></files>
  <action>
    Test that the circadian automation is visible and controllable via MCP:

    1. Ask Claude Android: "Is the circadian lighting automation on?"
    2. Expected: Claude reports the automation state (on/off)

    3. Ask Claude: "Turn off the circadian lighting automation"
    4. Then: "Turn it back on"
    5. Verify the automation state changes in HA UI

    This confirms that automation entities are exposed and controllable through MCP.
  </action>
  <verify>echo "Automation MCP test — user confirmation required"</verify>
  <done>Circadian automation visible and controllable via Claude Android MCP</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] MCP endpoint responds to POST requests with tool list
- [ ] Claude.ai custom connector configured with HA endpoint
- [ ] Claude Android shows HA tools in conversation
- [ ] Light toggle command physically affects Hue light (user visual confirmation)
- [ ] CO2 query returns reasonable sensor value
- [ ] Circadian automation visible and toggleable via MCP
</verification>

<success_criteria>
- "Turn off the lights" from Claude Android -> physical light turns off
- "What's the CO2 level?" from Claude Android -> returns ppm value
- Full end-to-end MCP working: Android -> claude.ai -> MCP -> Tailscale -> HA -> Device
</success_criteria>

<output>
After completion, create .planning/phases/46-big-push-deploy-integrate-and-test-all-recent-work/46-10-SUMMARY.md
</output>

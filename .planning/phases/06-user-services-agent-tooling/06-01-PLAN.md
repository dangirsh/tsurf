---
phase: 06-user-services-agent-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/syncthing.nix
  - modules/default.nix
autonomous: true

must_haves:
  truths:
    - "Syncthing service is enabled and runs as user dangirsh"
    - "4 devices are declared with placeholder IDs in the NixOS config"
    - "Single 'Sync' folder is declared with send-receive type and staggered versioning"
    - "Syncthing web UI is only accessible via Tailscale (firewall restricts port 8384)"
    - "nix flake check passes with the new syncthing module"
  artifacts:
    - path: "modules/syncthing.nix"
      provides: "Declarative Syncthing service configuration"
      contains: "services.syncthing"
    - path: "modules/default.nix"
      provides: "Module index importing syncthing.nix"
      contains: "./syncthing.nix"
  key_links:
    - from: "modules/default.nix"
      to: "modules/syncthing.nix"
      via: "import"
      pattern: "\\./syncthing\\.nix"
    - from: "modules/syncthing.nix"
      to: "services.syncthing.settings"
      via: "NixOS module option"
      pattern: "services\\.syncthing"
    - from: "modules/networking.nix"
      to: "modules/syncthing.nix"
      via: "trustedInterfaces includes tailscale0 (already set)"
      pattern: "trustedInterfaces.*tailscale0"
---

<objective>
Create the Syncthing declarative NixOS module with 4 devices, 1 shared folder, staggered versioning, and Tailscale-only GUI access.

Purpose: Enable bidirectional file sync between the acfs server and 4 personal devices (MacBook Pro, DC-1, Pixel 10 Pro, Theda's MacBook Pro) using NixOS's built-in Syncthing module with fully declarative device/folder management.

Output: `modules/syncthing.nix` wired into the module index, passing `nix flake check`.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-user-services-agent-tooling/06-RESEARCH.md
@modules/default.nix
@modules/networking.nix
@flake.nix
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Syncthing NixOS module</name>
  <files>modules/syncthing.nix</files>
  <action>
Create `modules/syncthing.nix` with the following configuration:

1. **Service basics:**
   - `services.syncthing.enable = true`
   - `user = "dangirsh"`, `group = "users"`
   - `dataDir = "/home/dangirsh"`, `configDir = "/home/dangirsh/.config/syncthing"`
   - `openDefaultPorts = true` (TCP 22000 + UDP 21027 -- already allowed by firewall)

2. **GUI access -- Tailscale-only via firewall (per research Pitfall 2):**
   - `guiAddress = "0.0.0.0:8384"` (bind to all interfaces)
   - Do NOT bind to a specific Tailscale IP (timing issue: Tailscale may not be up when Syncthing starts)
   - Port 8384 is NOT in `networking.firewall.allowedTCPPorts`, so only reachable via `trustedInterfaces = [ "tailscale0" ]` (already configured in networking.nix)
   - This achieves Tailscale-only access without IP binding

3. **Declarative device/folder management:**
   - `overrideDevices = true`, `overrideFolders = true`
   - 4 devices in `settings.devices` with PLACEHOLDER IDs (user must replace before deploy):
     - "MacBook-Pro.local" = { id = "DEVICE-ID-PLACEHOLDER-1"; }
     - "DC-1" = { id = "DEVICE-ID-PLACEHOLDER-2"; }
     - "Pixel 10 Pro" = { id = "DEVICE-ID-PLACEHOLDER-3"; }
     - "MacBook-Pro-von-Theda.local" = { id = "DEVICE-ID-PLACEHOLDER-4"; }

4. **Single "Sync" folder (per user decision -- not multiple folders):**
   - Folder ID: "sync" (lowercase for Syncthing internal use)
   - Label: "Sync"
   - `path = "/home/dangirsh/Sync"`
   - `devices` = all 4 device names
   - `type = "sendreceive"` (NOT receive-only -- user explicitly chose bidirectional)
   - `rescanIntervalS = 60` (per research discretion recommendation)
   - Staggered versioning: `cleanInterval = "3600"`, `maxAge = "7776000"` (90 days, per research)

5. **Misc settings:**
   - `options.urAccepted = -1` (disable usage reporting)
   - Set `STNODEFAULTFOLDER = "true"` via `systemd.services.syncthing.environment` to suppress default folder creation

6. **File header:** Add `@decision SVC-02` annotation and comment block explaining the Tailscale-only GUI approach.

Do NOT use the old `services.syncthing.declarative` syntax -- it was renamed. Use `services.syncthing.settings` with top-level `overrideDevices`/`overrideFolders`.
  </action>
  <verify>Read the file and confirm: services.syncthing.enable = true, overrideDevices = true, overrideFolders = true, 4 devices with placeholder IDs, 1 folder with sendreceive type and staggered versioning, guiAddress = "0.0.0.0:8384", STNODEFAULTFOLDER env var set.</verify>
  <done>modules/syncthing.nix exists with complete declarative Syncthing configuration matching all locked decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Syncthing module and validate</name>
  <files>modules/default.nix</files>
  <action>
Add `./syncthing.nix` to the imports list in `modules/default.nix`. Place it after `./docker.nix` and before `./agent-compute.nix` to maintain logical grouping (services together).

After modifying, run `nix flake check` to validate the entire configuration still evaluates cleanly with the new Syncthing module.

If `nix flake check` fails, debug the error and fix. Common issues:
- Attribute name typos in the Syncthing settings
- Missing quotes on string values in versioning params
- Wrong option path (e.g., using old `declarative` syntax)

Remember: new files must be `git add`ed before `nix flake check` (flakes only see tracked files).
  </action>
  <verify>`git add modules/syncthing.nix modules/default.nix && nix flake check` passes with exit code 0.</verify>
  <done>`nix flake check` passes. modules/default.nix imports syncthing.nix. The Syncthing module is fully integrated into the NixOS configuration.</done>
</task>

</tasks>

<verification>
- [ ] `modules/syncthing.nix` exists with services.syncthing configuration
- [ ] 4 devices declared with placeholder IDs
- [ ] 1 "Sync" folder with sendreceive type and staggered versioning
- [ ] GUI bound to 0.0.0.0:8384 (Tailscale-only via firewall, not IP binding)
- [ ] STNODEFAULTFOLDER environment variable set
- [ ] `modules/default.nix` imports `./syncthing.nix`
- [ ] `nix flake check` passes
</verification>

<success_criteria>
Syncthing is fully declared in NixOS config with 4 devices (placeholder IDs), 1 bidirectional "Sync" folder with staggered versioning, Tailscale-only GUI access, and the flake evaluates cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/06-user-services-agent-tooling/06-01-SUMMARY.md`
</output>

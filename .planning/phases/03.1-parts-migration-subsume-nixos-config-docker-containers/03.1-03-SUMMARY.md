---
phase: 03.1-parts-migration
plan: 03
subsystem: infra
tags: [nixos-module, oci-containers, sops-nix, sops-templates, docker-networks, systemd, flake-input]

requires:
  - phase: 03.1-plan-01
    provides: sops-nix secrets (secrets/parts.yaml), flake.nix with nixosModules.default
  - phase: 03.1-plan-02
    provides: nix/parts-agent.nix and nix/parts-tools.nix Docker image expressions
provides:
  - NixOS module declaring 2 containers, 2 networks, 10 secrets, 2 sops templates
  - Agent-neurosys flake integration importing parts module
  - Full nix flake check validation of the integration
affects: [phase-03-networking-secrets-docker, phase-04-docker-services]

tech-stack:
  added: []
  patterns: [sops-templates-for-env-files, oci-containers-with-imageFile, systemd-network-ordering]

key-files:
  created:
    - /data/projects/parts/nix/module.nix
  modified:
    - /data/projects/neurosys/flake.nix
    - /data/projects/neurosys/flake.lock

key-decisions:
  - "sops.templates render secrets into env files using sops.placeholder — consumed by oci-containers environmentFiles"
  - "Module does NOT import sops-nix, set defaultSopsFile, enable Docker, or declare system-level config"
  - "Per-secret sopsFile overrides with self + /secrets/parts.yaml — not defaultSopsFile"
  - "path: flake input for local development — change to github: for production deployment"

patterns-established:
  - "sops.templates for container env files: render secrets via sops.placeholder references"
  - "oci-containers imageFile: locally-built images via pkgs.callPackage (no registry)"
  - "Systemd network ordering: containers require + after network oneshot services"
  - "Cross-flake module integration: inputs.X.nixosModules.default with follows for shared deps"

duration: ~15min
completed: 2026-02-15
---

# Phase 3.1 Plan 03: NixOS Module + Agent-Neurosys Flake Integration

**NixOS module declaring 2 containers, 2 networks, 10 sops secrets, and 2 env templates — integrated into neurosys via flake input with `nix flake check` passing**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-02-15
- **Completed:** 2026-02-15
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Created 200-line NixOS module (nix/module.nix) with complete container, network, and secrets declarations
- Integrated parts as flake input in neurosys with nixpkgs + sops-nix follows
- `nix flake check` passes for neurosys — validates full module evaluation including secrets, containers, and networks
- Proper systemd ordering: containers start after network services, tmpfiles create host directories

## Task Commits

1. **Task 1: Create nix/module.nix** - `594a361` (feat, in parts repo)
2. **Task 2: Integrate parts flake input** - `a6db078` (feat, in neurosys repo)

## Files Created/Modified
- `/data/projects/parts/nix/module.nix` - NixOS module with curried pattern: secrets, templates, networks, containers, ordering, tmpfiles
- `/data/projects/neurosys/flake.nix` - Added parts input with follows, imported nixosModules.default
- `/data/projects/neurosys/flake.lock` - Pinned parts flake

## Decisions Made
- Used `path:/data/projects/parts` for local development (must change to `github:dangirsh/parts` for production)
- agent_net is `--internal` (no external access), tools_net is external (needs outbound for APIs)
- Container env vars split: non-secret values in `environment`, secret values via `environmentFiles` from sops.templates
- parts-agent depends on parts-tools (gateway must be running first)

## Deviations from Plan
None - plan executed as written. Both tasks completed without issues.

## Issues Encountered
- `.claude/.test-status` file needed for commit hook — had to create `.claude/` directory in worktree before commit would succeed

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 3.1 complete — parts repo exports a NixOS module, neurosys imports it
- Ready for Phase 2 (bootable base system) or Phase 3 (networking + Docker foundation)
- Before deployment: change parts flake input from `path:` to `github:` URL

---
*Phase: 03.1-parts-migration*
*Completed: 2026-02-15*

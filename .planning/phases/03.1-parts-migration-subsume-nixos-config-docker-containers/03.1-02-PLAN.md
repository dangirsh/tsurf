---
phase: 03.1-parts-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /data/projects/parts/nix/parts-agent.nix
  - /data/projects/parts/nix/parts-tools.nix
autonomous: true

must_haves:
  truths:
    - "parts-agent Docker image can be built via nix-build and loaded with docker load"
    - "parts-tools Docker image can be built via nix-build and loaded with docker load"
    - "Both images contain the correct Node.js application code compiled from TypeScript"
    - "parts-tools image contains Java 21, signal-cli, git, and ripgrep runtime dependencies"
    - "Neither image uses runAsRoot (no KVM requirement)"
  artifacts:
    - path: "/data/projects/parts/nix/parts-agent.nix"
      provides: "Nix expression for parts-agent Docker image"
      contains: "buildLayeredImage"
      min_lines: 30
    - path: "/data/projects/parts/nix/parts-tools.nix"
      provides: "Nix expression for parts-tools Docker image"
      contains: "buildLayeredImage"
      min_lines: 40
  key_links:
    - from: "/data/projects/parts/nix/parts-agent.nix"
      to: "/data/projects/parts/agent/package.json"
      via: "buildNpmPackage with npmWorkspace"
      pattern: "npmWorkspace.*agent"
    - from: "/data/projects/parts/nix/parts-tools.nix"
      to: "/data/projects/parts/gateway/package.json"
      via: "buildNpmPackage with npmWorkspace"
      pattern: "npmWorkspace.*gateway"
    - from: "/data/projects/parts/nix/parts-tools.nix"
      to: "nixpkgs signal-cli and temurin-jre-bin-21"
      via: "contents list in buildLayeredImage"
      pattern: "signal-cli|temurin"
---

<objective>
Create Nix expressions that build parts-agent and parts-tools Docker images using dockerTools.buildLayeredImage.

Purpose: Replace the existing Dockerfiles with pure Nix image builds. These images are reproducible (no Docker daemon needed at build time), cacheable (multi-layer for incremental rebuilds), and integrated with nixos-rebuild. The expressions are standalone callPackage-able files that the parts NixOS module (Plan 03) will reference via `imageFile`.

Output: `nix/parts-agent.nix` and `nix/parts-tools.nix` — two Nix expressions that produce Docker image tarballs.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.1-parts-migration-subsume-nixos-config-docker-containers/03.1-RESEARCH.md

# Source files — understand the apps being containerized
@/data/projects/parts/containers/agent/Dockerfile
@/data/projects/parts/containers/tools/Dockerfile
@/data/projects/parts/package.json
@/data/projects/parts/agent/package.json
@/data/projects/parts/gateway/package.json
@/data/projects/parts/agent/tsconfig.json
@/data/projects/parts/gateway/tsconfig.json
@/data/projects/parts/shared/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create nix/parts-agent.nix — agent container image</name>
  <files>/data/projects/parts/nix/parts-agent.nix</files>
  <action>
Create directory `/data/projects/parts/nix/` if it doesn't exist.

Create `nix/parts-agent.nix` — a `callPackage`-compatible Nix expression: `{ pkgs, src, ... }:`.

This expression must:

1. **Build the Node.js app with `buildNpmPackage`:**
   - `pname = "parts-agent"`, `version = "0.1.0"`
   - `src = src` (entire repo root — npm workspaces need root package.json and package-lock.json)
   - `npmDepsHash = "sha256-PLACEHOLDER"` — after writing the file, compute the real hash by running `nix-store --store dummy:// --query --hash $(nix-build -E 'with import <nixpkgs> {}; prefetch-npm-deps ./package-lock.json')` OR `prefetch-npm-deps /data/projects/parts/package-lock.json` (the prefetch-npm-deps command prints the hash). Update the placeholder with the real hash.
   - `npmWorkspace = "agent"` — builds the agent workspace specifically
   - `npmBuildScript = "build"` — runs `npm run build` (which runs `tsc`)
   - `nodejs = pkgs.nodejs_22` — ensure Node.js 22 is used for build
   - Custom `installPhase` that copies:
     - `agent/dist/` to `$out/lib/parts-agent/dist/` (compiled TypeScript output)
     - `node_modules/` from the workspace to `$out/lib/parts-agent/node_modules/`
     - `shared/` directory to `$out/lib/parts-agent/shared/` (shared types, referenced at runtime via rootDir ".." compilation)
     - `agent/src/config/SOUL.md` to `$out/lib/parts-agent/dist/agent/src/config/SOUL.md` (non-TS asset copied by postbuild script, but may need manual copy in Nix since postbuild may not run in installPhase context)

2. **Build the Docker image with `buildLayeredImage`:**
   - `name = "parts-agent"`, `tag = "latest"`
   - `contents` includes: `dockerTools.caCertificates`, `dockerTools.fakeNss` (for HTTPS and user resolution)
   - `config.Cmd` runs Node.js 22 pointing at the compiled entrypoint: `${pkgs.nodejs_22}/bin/node ${parts-agent-app}/lib/parts-agent/dist/agent/src/index.js`
   - `config.Env` sets: NODE_ENV=production, GATEWAY_URL=http://parts-tools:8080, AGENT_NAME=Jarvis, THINKING_BUDGET=10000, SESSION_DIR=/data/sessions, SESSION_LOG=/data/session-log.jsonl
   - `config.WorkingDir` = `"${parts-agent-app}/lib/parts-agent"`
   - Do NOT use `runAsRoot` (requires KVM). Use `fakeRootCommands` or `extraCommands` if needed for /data directory creation, or handle volume mounts at the container level (preferred — the host creates /var/lib/parts/sessions via tmpfiles).
   - No `maxLayers` override needed for the simpler agent image.

**Important npm workspace nuances:**
- The root `package-lock.json` (at `/data/projects/parts/package-lock.json`) covers both workspaces. Use this for `npmDepsHash`.
- The `shared/` package has its own `package.json` but may not be listed in `workspaces` (check — the root package.json only lists "gateway" and "agent"). If shared has its own deps, they need to be available.
- The agent's `tsconfig.json` uses `rootDir: ".."` which means compiled output goes to `dist/agent/src/` and `dist/shared/`. Make sure the installPhase accounts for this directory structure.

**Hashing the npm deps:**
After writing the file with PLACEHOLDER, compute the real hash:
```bash
cd /data/projects/parts
nix-shell -p prefetch-npm-deps --run "prefetch-npm-deps package-lock.json"
```
Then update the `npmDepsHash` in the file with the returned hash.
  </action>
  <verify>
1. File exists at `/data/projects/parts/nix/parts-agent.nix`
2. Contains `buildNpmPackage` with `npmWorkspace = "agent"`
3. Contains `buildLayeredImage` with `name = "parts-agent"`
4. Does not contain `runAsRoot`
5. `npmDepsHash` is NOT "sha256-PLACEHOLDER" (real hash computed)
6. Attempt to build: `cd /data/projects/parts && nix-build -E 'with import <nixpkgs> {}; callPackage ./nix/parts-agent.nix { src = ./.; }'` — if this succeeds, the image tarball is at `./result`. If it fails, diagnose and fix the Nix expression (common issues: hash mismatch, missing build deps, installPhase path errors).
  </verify>
  <done>
- `/data/projects/parts/nix/parts-agent.nix` exists with a working buildNpmPackage + buildLayeredImage expression
- `npmDepsHash` contains a real sha256 hash (not placeholder)
- The expression produces a Docker image tarball that can be loaded with `docker load`
- The image contains the compiled parts-agent Node.js application
  </done>
</task>

<task type="auto">
  <name>Task 2: Create nix/parts-tools.nix — tools/gateway container image</name>
  <files>/data/projects/parts/nix/parts-tools.nix</files>
  <action>
Create `nix/parts-tools.nix` — a `callPackage`-compatible Nix expression: `{ pkgs, src, ... }:`.

This is the complex image. The current Dockerfile (containers/tools/Dockerfile) is a 4-stage build with:
- Eclipse Temurin JRE 21 (for signal-cli JNI compatibility)
- signal-cli 0.13.24 downloaded from GitHub releases
- Node.js 22, git, ripgrep installed via apt

The Nix expression must:

1. **Build the Node.js app with `buildNpmPackage`:**
   - `pname = "parts-tools"`, `version = "0.1.0"`
   - `src = src` (entire repo root)
   - `npmDepsHash` — use the SAME hash as parts-agent.nix (same root package-lock.json)
   - `npmWorkspace = "gateway"` — builds the gateway workspace
   - `npmBuildScript = "build"`
   - `nodejs = pkgs.nodejs_22`
   - `nativeBuildInputs = [ pkgs.git ]` — git may be needed during npm install for some dependencies
   - Custom `installPhase`:
     - `gateway/dist/` to `$out/lib/parts-tools/dist/`
     - Workspace `node_modules/` to `$out/lib/parts-tools/node_modules/`
     - `shared/` to `$out/lib/parts-tools/shared/`
     - `gateway/src/config/` to `$out/lib/parts-tools/src/config/` (non-TS config assets — the Dockerfile has `COPY gateway/src/config ./src/config`)

2. **Build the Docker image with `buildLayeredImage`:**
   - `name = "parts-tools"`, `tag = "latest"`
   - `maxLayers = 100` (more layers = better caching for this complex image)
   - `contents` includes:
     - `dockerTools.caCertificates` (HTTPS)
     - `dockerTools.fakeNss` (user resolution)
     - `pkgs.nodejs_22` (runtime)
     - `pkgs.git` (used by tools at runtime for file operations)
     - `pkgs.ripgrep` (used by tools at runtime for search)
     - `pkgs.temurin-jre-bin-21` (Java runtime for signal-cli)
     - `pkgs.signal-cli` (Signal messaging — use nixpkgs package instead of manual download; if version differs from 0.13.24, that's acceptable as long as it works)
     - `pkgs.coreutils` (basic Unix utilities)
   - `config.Cmd` runs: `${pkgs.nodejs_22}/bin/node ${parts-tools-app}/lib/parts-tools/dist/gateway/src/index.js`
   - `config.Env` sets:
     - `NODE_ENV=production`
     - `JAVA_HOME=${pkgs.temurin-jre-bin-21}` (signal-cli needs this)
     - `PATH` set via `pkgs.lib.makeBinPath` including nodejs_22, git, ripgrep, temurin-jre-bin-21, signal-cli, coreutils
   - `config.ExposedPorts = { "8080/tcp" = {}; }`
   - `config.WorkingDir = "${parts-tools-app}/lib/parts-tools"`
   - Do NOT use `runAsRoot`

**signal-cli note:** The nixpkgs `signal-cli` package bundles its own Java dependency. Verify whether it uses the system JAVA_HOME or its own bundled JRE. If it bundles its own, `temurin-jre-bin-21` may not be needed in contents (but keep JAVA_HOME set in case any other Java tool needs it). Check with: `nix-shell -p signal-cli --run "signal-cli --version"` to confirm availability.

**Important:** The gateway workspace's `package.json` has native deps like `better-sqlite3` which require build tools. Add `pkgs.python3` and `pkgs.gnumake` and `pkgs.gcc` (or `pkgs.stdenv.cc`) to `nativeBuildInputs` if the build fails on native addon compilation.
  </action>
  <verify>
1. File exists at `/data/projects/parts/nix/parts-tools.nix`
2. Contains `buildNpmPackage` with `npmWorkspace = "gateway"`
3. Contains `buildLayeredImage` with `name = "parts-tools"`
4. Contains `signal-cli` and `temurin-jre-bin-21` in contents
5. Contains `git` and `ripgrep` in contents
6. Does not contain `runAsRoot`
7. Attempt to build: `cd /data/projects/parts && nix-build -E 'with import <nixpkgs> {}; callPackage ./nix/parts-tools.nix { src = ./.; }'` — diagnose and fix if it fails. Common issues: native addon build failures (need python3/make/gcc in nativeBuildInputs), signal-cli version incompatibility, missing git during npm install.
  </verify>
  <done>
- `/data/projects/parts/nix/parts-tools.nix` exists with a working buildNpmPackage + buildLayeredImage expression
- The expression includes all runtime dependencies: Node.js 22, Java 21, signal-cli, git, ripgrep
- The image contains the compiled parts-tools (gateway) Node.js application
- Both parts-agent.nix and parts-tools.nix use the same npmDepsHash (shared root lockfile)
  </done>
</task>

</tasks>

<verification>
- Both nix files exist in /data/projects/parts/nix/
- Both use buildLayeredImage (not buildImage)
- Both use buildNpmPackage with correct npmWorkspace values
- Neither uses runAsRoot
- npmDepsHash is a real hash in both files
- Ideally, `nix-build` succeeds for both (may require iterating on build issues)
</verification>

<success_criteria>
1. `nix/parts-agent.nix` produces a Docker image tarball with the parts-agent Node.js app
2. `nix/parts-tools.nix` produces a Docker image tarball with the parts-tools (gateway) app plus Java, signal-cli, git, ripgrep
3. Both images use buildLayeredImage for multi-layer caching
4. Both are callPackage-compatible (`{ pkgs, src, ... }:` signature)
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-parts-migration-subsume-nixos-config-docker-containers/03.1-02-SUMMARY.md`
</output>

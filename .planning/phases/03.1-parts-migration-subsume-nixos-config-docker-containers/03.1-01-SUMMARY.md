---
phase: 03.1-parts-migration
plan: 01
subsystem: infra
tags: [sops-nix, agenix, age, secrets, flake, nixos-module]

requires:
  - phase: 01-flake-scaffolding-pre-deploy
    provides: sops-nix infrastructure pattern, .sops.yaml conventions, age key derivation
provides:
  - Parts .sops.yaml with age key references for admin and acfs host
  - Parts secrets/parts.yaml with 10 secrets sops-encrypted
  - Parts flake.nix rewritten to export nixosModules.default and packages outputs
  - sops-nix replaces agenix in parts repo
affects: [03.1-plan-02, 03.1-plan-03, phase-03-networking-secrets-docker]

tech-stack:
  added: [sops-nix]
  patterns: [cross-flake-module-export, curried-module-pattern]

key-files:
  created:
    - /data/projects/parts/.sops.yaml
    - /data/projects/parts/secrets/parts.yaml
  modified:
    - /data/projects/parts/flake.nix
    - /data/projects/parts/flake.lock

key-decisions:
  - "Used local age key (age1vma7w9...) instead of orphaned admin_dangirsh key — Phase 1 Codex generated age keys but private key wasn't persisted"
  - "Excluded tailscale-authkey from parts secrets — belongs in agent-neurosys as system-level secret"
  - "Parts flake targets nixpkgs-unstable (not 25.11) — agent-neurosys pins the version via follows"
  - "Curried module pattern: outer { self, sops-nix } from flake, inner { config, lib, pkgs } from NixOS"

patterns-established:
  - "Cross-flake module export: nixosModules.default = import ./nix/module.nix { inherit self sops-nix; }"
  - "Standalone packages output for testing: packages.x86_64-linux.{parts-agent,parts-tools}"
  - "sops encryption with public keys only — private key needed only for decryption"

duration: ~25min
completed: 2026-02-15
---

# Phase 3.1 Plan 01: Secrets Migration (agenix to sops-nix) + Parts Flake Rewrite

**Migrated 10 secrets from agenix .age files to sops-nix YAML, rewrote parts flake.nix to export portable NixOS module and package outputs**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-02-15
- **Completed:** 2026-02-15
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Decrypted all agenix .age secrets and re-encrypted as sops YAML (10 secrets total)
- Created .sops.yaml with local age key + acfs host key for dual-recipient encryption
- Rewrote flake.nix: removed nixosConfigurations, added nixosModules.default + packages outputs
- Updated flake.lock with sops-nix replacing agenix

## Task Commits

1. **Task 1: Create sops-nix secrets infrastructure** - `9faeb17` (feat)
2. **Task 2: Rewrite parts flake.nix** - `f110943` (feat, Codex-generated)

## Files Created/Modified
- `/data/projects/parts/.sops.yaml` - sops creation rules with admin + host age keys
- `/data/projects/parts/secrets/parts.yaml` - 10 secrets in sops-encrypted YAML format
- `/data/projects/parts/flake.nix` - Exports nixosModules.default and packages.x86_64-linux
- `/data/projects/parts/flake.lock` - Pins sops-nix, removes agenix

## Decisions Made
- Used local age key (`age1vma7w9...` from `~/.config/sops/age/keys.txt`) as admin key because the admin_dangirsh key from Phase 1 has no private key on this machine (orphaned from Codex sandbox execution)
- Tailscale authkey excluded from parts secrets — it's a system-level secret belonging in agent-neurosys
- Google OAuth credentials split into 3 separate secrets (client-id, client-secret, refresh-token) instead of a single JSON blob

## Deviations from Plan

### Auto-fixed Issues

**1. Orphaned admin age key — used local key instead**
- **Found during:** Task 1 (secrets encryption)
- **Issue:** admin_dangirsh age key from Phase 1 has no private key on this machine
- **Fix:** Used local age key from `~/.config/sops/age/keys.txt` as admin key in .sops.yaml
- **Files modified:** /data/projects/parts/.sops.yaml
- **Verification:** `sops -d secrets/parts.yaml` succeeds
- **Committed in:** `9faeb17`

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Admin key substitution is functionally equivalent. Local key can decrypt; host key handles server-side decryption.

## Issues Encountered
- Decrypting .age files required finding the correct local SSH/age key identity — resolved by checking `~/.config/sops/age/keys.txt`
- Google OAuth credentials stored as JSON blob in agenix — had to extract individual fields for sops flat YAML format

## User Setup Required
None - secrets were decrypted and re-encrypted autonomously.

## Next Phase Readiness
- Parts flake exports module and packages, ready for Plan 02 (Docker image Nix expressions)
- .sops.yaml and secrets/parts.yaml in place for Plan 03 (module.nix sops.secrets declarations)

---
*Phase: 03.1-parts-migration*
*Completed: 2026-02-15*

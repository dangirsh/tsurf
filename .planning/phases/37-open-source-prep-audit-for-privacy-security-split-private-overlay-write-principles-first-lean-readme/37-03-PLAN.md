---
phase: 37-open-source-prep-audit-for-privacy-security-split-private-overlay-write-principles-first-lean-readme
plan: 03
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - README.md
autonomous: true

must_haves:
  truths:
    - "README.md opens with a single-line description of what neurosys is — no preamble"
    - "README.md does not mention any personal identifiers (IPs, username dangirsh, SSH keys, real name)"
    - "README.md leads with a design philosophy section after the one-liner"
    - "README.md is table-heavy and bullet-heavy — minimal prose paragraphs"
    - "README.md highlights agent tooling (agent-spawn, bubblewrap, secret proxy, llm-agents overlay) as the primary differentiator"
    - "README.md mentions the private overlay pattern and links to docs/private-overlay.md"
    - "README.md is under 150 lines"
    - "All operational content specific to personal deployments (Contabo VPS specs, OVH static IPs, recovery runbook private specifics) is removed"
  artifacts:
    - path: "README.md"
      provides: "Lean public-facing README for open source release"
      min_lines: 60
      contains: "agent-spawn"
  key_links:
    - from: "README.md"
      to: "docs/private-overlay.md"
      via: "link to private overlay guide"
      pattern: "private-overlay"
---

<objective>
Rewrite README.md as a lean, public-facing document for the open source audience.

Purpose: The current README is an operator's manual written for the solo owner — full of personal IPs, Contabo/OVH specifics, and deployment war stories. The public README should be immediately useful to a Linux developer who lands on the repo, showing what this is, why it's interesting, and how to use it — without any personal config details.

Output: A rewritten README.md under 150 lines, table-heavy, principle-first, with the agent tooling front-and-center as the primary value proposition. Style reference: stereos README (extremely sparse, immediate, technical).
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@README.md
@modules/agent-compute.nix
@modules/networking.nix
@.planning/phases/37-open-source-prep-audit-for-privacy-security-split-private-overlay-write-principles-first-lean-readme/37-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md — lean, principle-first, table-heavy</name>
  <files>README.md</files>
  <action>
    Replace the entire README.md with a new document. The structure and content rules:

    **Line 1**: A single-sentence description. Example: "NixOS server config for an agentic development platform — declarative, batteries-included, Tailscale-only networking."

    **Section: Design Principles** (immediately after the one-liner, no preamble):
    A tight bullet list of 5-6 principles. These are the philosophical commitments that make this config interesting:
    - Declarative everything — no imperative setup steps after deploy
    - Tailscale-only internal networking — all services Tailscale-only by default, public firewall minimal
    - Agents as first-class workloads — bubblewrap sandboxing, cgroup isolation, multi-provider key injection
    - Secrets never in the Nix store — sops-nix age-encrypted, decrypted at activation
    - Impermanent root — BTRFS ephemeral / subvolume, explicit /persist state manifest
    - Private config via overlay — personal services and secrets stay in a private flake repo

    **Section: Modules** (table):
    A clean table of all PUBLIC modules (the ones remaining in modules/default.nix after Plan 01). Columns: Module | Role | Key detail. Keep it to one line per module. Modules to include: base.nix, boot.nix, users.nix, networking.nix, secrets.nix, docker.nix, monitoring.nix, syncthing.nix, agent-compute.nix, secret-proxy.nix, impermanence.nix, restic.nix, homepage.nix.

    **Section: Agent Tooling** (the differentiator — slightly more detail):
    Two sub-parts:
    1. A bullet list of what's included: claude-code, codex, opencode, gemini-cli, pi — all via llm-agents.nix overlay. Plus zmx (session persistence), agent-spawn (launcher).
    2. A code block showing the agent-spawn usage: `agent-spawn <name> <project-dir> [claude|codex|opencode|gemini|pi] [--no-sandbox]`
    3. A table showing sandbox policy: what's visible (ro), what's writable, what's hidden. Derive from the actual bwrap args in agent-compute.nix.

    **Section: Secret Proxy**:
    2-3 sentences + one table row: real API keys read pre-sandbox from /run/secrets, injected as env vars into sandbox. Secret proxy on :9091 lets specific projects use a forwarding proxy instead of the real key directly — the real key never enters the sandbox environment for those projects.

    **Section: Networking**:
    One table: Port | Service | Access level. Derive from networking.nix internalOnlyPorts and allowedTCPPorts. Only list ports from public modules. Keep to the facts: 22/80/443/22000 public, rest Tailscale-only or localhost-only.

    **Section: Quick Start**:
    Minimal. 4 numbered steps:
    1. Fork this repo. Set up your private overlay (see docs/private-overlay.md).
    2. Generate age key from your SSH host key. Encrypt secrets with sops.
    3. nixos-anywhere to install. deploy-rs to deploy updates.
    4. agent-spawn to launch agents.
    No hand-holding. Technical users can read the flake.

    **Section: Inputs**:
    Table of flake inputs (public ones only after Plan 01). Columns: Input | Source | Purpose. Should have ~9 rows.

    **No sections for**: personal deployment ops (Contabo specs, OVH bootstrap steps), recovery runbook, accepted risks table, design decisions table. Those move to the private overlay or docs/.

    **Total target**: under 150 lines. Dense. No paragraph-length prose except the one-liner and maybe 1-2 sentence transitions.

    Verify the final README.md contains no personal identifiers by grepping for: dangirsh, 161.97, 135.125, dan@, Dan Girshovich, claw-swap.com, dangirsh.org.
  </action>
  <verify>wc -l /data/projects/neurosys/README.md && grep -c "agent-spawn\|bubblewrap\|sops-nix\|Tailscale" /data/projects/neurosys/README.md && grep -iE "dangirsh|161\.97|135\.125|dan@|Dan Girshovich" /data/projects/neurosys/README.md && echo "PII check done"</verify>
  <done>README.md is between 60-150 lines, mentions agent-spawn and Tailscale, and grep for PII returns zero matches</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] wc -l README.md shows between 60 and 150 lines
- [ ] grep -iE "dangirsh|161\.97|135\.125|dan@worldcoin|dan\.girshovich|Dan Girshovich" README.md returns zero results
- [ ] README.md contains a modules table with at least 8 rows
- [ ] README.md contains a sandbox policy table derived from agent-compute.nix bwrap args
- [ ] README.md links to docs/private-overlay.md
- [ ] README.md opens with a one-line description (no lengthy preamble)
</verification>

<success_criteria>
- README.md rewritten and committed
- Under 150 lines, principle-first, table-heavy
- Agent tooling section is the most detailed section (primary value prop)
- Zero personal identifiers in the document
- Links to private-overlay.md for personal config guidance
</success_criteria>

<output>
After completion, create .planning/phases/37-open-source-prep-audit-for-privacy-security-split-private-overlay-write-principles-first-lean-readme/37-03-SUMMARY.md
</output>

---
phase: 37-open-source-prep-audit-for-privacy-security-split-private-overlay-write-principles-first-lean-readme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flake.nix
  - modules/default.nix
  - modules/users.nix
  - modules/networking.nix
  - modules/agent-compute.nix
  - modules/syncthing.nix
  - modules/repos.nix
  - modules/homepage.nix
  - home/git.nix
  - home/agent-config.nix
  - hosts/neurosys/default.nix
  - hosts/ovh/default.nix
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "No IP addresses appear anywhere in the public-facing files (grep -r '161\.97\|135\.125\|213\.136' returns zero results outside secrets/)"
    - "No real SSH public keys appear in users.nix — only example placeholder lines"
    - "No personal email addresses or real names appear in git.nix or anywhere in modules/"
    - "Username 'dangirsh' does not appear in any module or home file — replaced with 'myuser' everywhere"
    - "Private repo URLs (dangirsh/personal-agent-runtime, dangirsh/claw-swap, dangirsh/dangirsh.org) do not appear in flake.nix"
    - ".gitignore includes .planning/, secrets/, .sops.yaml, and .worktrees/"
    - "modules/default.nix does not import private modules (home-assistant, spacebot, automaton, openclaw, matrix, repos, nginx)"
    - "nix flake check passes after all changes"
  artifacts:
    - path: "flake.nix"
      provides: "Public flake with placeholder inputs for private services and exports nixosModules.default"
      contains: "nixosModules"
    - path: "modules/default.nix"
      provides: "Public module import hub — only public modules"
      min_lines: 5
    - path: "modules/users.nix"
      provides: "User config with placeholder username and example SSH key"
      contains: "myuser"
    - path: ".gitignore"
      provides: "Excludes private files from public repo"
      contains: ".planning"
  key_links:
    - from: "flake.nix"
      to: "modules/default.nix"
      via: "nixosModules.default export"
      pattern: "nixosModules"
    - from: "modules/users.nix"
      to: "modules/agent-compute.nix"
      via: "username placeholder consistency"
      pattern: "myuser"
---

<objective>
Perform the full privacy audit on the neurosys repo and make it safe for public release.

Purpose: Remove all personal identifiers (IPs, username, SSH keys, real names, private repo URLs, secrets) from every tracked file. Remove private-concern modules from the public module set. Export public modules as nixosModules for private overlay consumption. Add .gitignore entries to permanently exclude private files.

Output: A git-committed set of changes that leaves the repo free of PII and private service specifics while preserving all generic infrastructure patterns (networking, agent-compute, monitoring, restic, sops pattern, etc.).
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@flake.nix
@modules/default.nix
@modules/users.nix
@modules/networking.nix
@modules/agent-compute.nix
@modules/syncthing.nix
@modules/repos.nix
@modules/homepage.nix
@home/git.nix
@home/agent-config.nix
@hosts/neurosys/default.nix
@hosts/ovh/default.nix
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scrub IPs, real name, SSH keys from host files and users.nix</name>
  <files>hosts/neurosys/default.nix, hosts/ovh/default.nix, modules/users.nix, home/git.nix</files>
  <action>
    In hosts/neurosys/default.nix: Remove the static IP block entirely (networking.interfaces.eth0.ipv4.addresses, networking.defaultGateway, networking.nameservers). Replace with a comment: "# Static IP config is host-specific — set in your private overlay or directly here." Keep networking.useDHCP = false but remove the interface address block. This is the Contabo-specific IP that must not be public.

    In hosts/ovh/default.nix: No IP address is hardcoded (it uses DHCP). No changes needed for IPs here. Keep as-is.

    In modules/users.nix: Replace the username "dangirsh" with "myuser" throughout. Replace both SSH public key strings with placeholder comments: "# Replace with your SSH public key". Replace the @decision comment reference to "dangirsh". Keep the structure (wheel + docker groups, subUidRanges, subGidRanges, passwordless wheel sudo, execWheelOnly) — these are the generic patterns worth keeping.

    In home/git.nix: Replace userName "Dan Girshovich" with "Your Name" and userEmail "dan.girshovich@gmail.com" with "your@email.com". Keep the gh program enable and its comment.

    Result: No real IPs, no real SSH keys, no real personal name or email in any tracked file.
  </action>
  <verify>grep -r "161\.97\|135\.125\|213\.136\|dan@worldcoin\|dan.girshovich\|Dan Girshovich\|AAAAC3NzaC1lZDI1" /data/projects/neurosys/hosts /data/projects/neurosys/modules/users.nix /data/projects/neurosys/home/git.nix</verify>
  <done>grep returns zero results — no personal IPs, SSH keys, or real name/email in those files</done>
</task>

<task type="auto">
  <name>Task 2: Abstract username and private repos from modules and home files</name>
  <files>modules/agent-compute.nix, modules/syncthing.nix, modules/repos.nix, modules/home-assistant.nix, home/agent-config.nix</files>
  <action>
    In modules/agent-compute.nix: Replace all hardcoded "dangirsh" path strings with "myuser". Affected lines: --ro-bind-try /etc/profiles/per-user/dangirsh, --ro-bind-try /home/dangirsh/.nix-profile, all /home/dangirsh/* bind paths, --setenv HOME /home/dangirsh, --setenv USER dangirsh, the PATH line with /etc/profiles/per-user/dangirsh, the sandbox policy echo block. Also replace the claw-swap-specific secret proxy branch: change `if [[ "$PROJECT_DIR" == /data/projects/claw-swap* ]]` to a comment explaining that the private overlay wires the secret proxy for specific projects. Remove that private-service-specific if/else and replace it with a comment block:
      "# Private overlay: wire ANTHROPIC_BASE_URL for projects using the secret proxy.
       # Example: if [[ \"$PROJECT_DIR\" == /data/projects/my-project* ]]; then
       #   BWRAP_ARGS+=( --setenv ANTHROPIC_BASE_URL \"http://127.0.0.1:9091\" )
       # fi"
    Then unconditionally inject ANTHROPIC_KEY if set (no claw-swap special-casing).
    Also replace "d /data/projects/.agent-audit 0750 dangirsh users -" with "myuser".
    Replace users.users.dangirsh.linger = true with users.users.myuser.linger = true.

    In modules/syncthing.nix: Replace "dangirsh" username, group "users", dataDir/configDir paths with "myuser". Replace device IDs (LYQPMIK-... and YBHZJDE-...) with placeholder strings: "REPLACE-WITH-YOUR-DEVICE-ID-A" and "REPLACE-WITH-YOUR-DEVICE-ID-B". Replace device names "MacBook-Pro.local" and "Pixel 10 Pro" with "my-laptop" and "my-phone". Replace folder path "/home/dangirsh/Sync" with "/home/myuser/Sync".

    In modules/repos.nix: Replace private repo list with placeholder examples:
      "example-user/my-service-a"
      "example-user/my-service-b"
    Replace CLONE_DIR chown "dangirsh:users" with "myuser:users". Keep the git credential store pattern intact (the mechanism is the public value, not the repo list).

    In modules/home-assistant.nix: Replace "dangirsh/home-assistant-config.git" with "example-user/home-assistant-config.git". Replace chown "hass:hass" (this is fine, HA's own user). Replace "dangirsh" in any comments with "example-user".

    In home/agent-config.nix: Replace "/data/projects/global-agent-conf" with "/data/projects/global-agent-conf" (this path is generic enough — keep as-is since it's not a private repo name). Keep the symlink pattern.

    Result: No "dangirsh" username anywhere in tracked Nix files. Private service repos replaced with placeholders. The credential store pattern (public value: git token injected safely) is preserved.
  </action>
  <verify>grep -r "dangirsh" /data/projects/neurosys/modules /data/projects/neurosys/home /data/projects/neurosys/hosts /data/projects/neurosys/flake.nix</verify>
  <done>grep returns zero results — "dangirsh" does not appear in any module, home, host, or flake file</done>
</task>

<task type="auto">
  <name>Task 3: Scrub private inputs from flake.nix, remove private modules, update .gitignore, export nixosModules</name>
  <files>flake.nix, modules/default.nix, .gitignore</files>
  <action>
    In flake.nix:
    - Remove the "parts" input (github:dangirsh/personal-agent-runtime) entirely.
    - Remove the "claw-swap" input (github:dangirsh/claw-swap) entirely.
    - Remove the "dangirsh-site" input (github:dangirsh/dangirsh.org) entirely.
    - Remove the "automaton" input (github:Conway-Research/automaton, flake = false) entirely.
    - Keep: nixpkgs, home-manager, sops-nix, disko, llm-agents, deploy-rs, impermanence, srvos, treefmt-nix.
    - In outputs, remove inputs.parts.nixosModules.default and inputs.claw-swap.nixosModules.default from commonModules.
    - Remove dangirsh-site from outputs destructure.
    - Remove automaton from packages.${system}.
    - Update home-manager.users reference from "dangirsh" to "myuser".
    - Add a nixosModules output: nixosModules.default = import ./modules; — so private overlays can consume public modules as a flake input.
    - Add a comment at the top of outputs: "# Private overlay: add your private inputs (parts, personal services, etc.) in a separate private flake that imports this one."

    In modules/default.nix:
    - Remove imports for private-concern modules: home-assistant.nix, spacebot.nix, automaton.nix, openclaw.nix, matrix.nix, repos.nix.
    - Keep: base.nix, boot.nix, users.nix, networking.nix, secrets.nix, docker.nix, monitoring.nix, syncthing.nix, agent-compute.nix, secret-proxy.nix, impermanence.nix, restic.nix, homepage.nix.
    - Note: nginx.nix is already host-specific (only imported in hosts/ovh/default.nix), not in default.nix — leave that alone.

    In .gitignore:
    - Add: .planning/
    - Add: secrets/
    - Add: .sops.yaml
    - Add: .worktrees/ (already there implicitly but make explicit)
    - Keep existing entries: tmp/, .claude/*, !.claude/skills/, !.claude/CLAUDE.md, result, *.png

    Result: flake.nix has no private repo inputs, exports nixosModules.default for private overlay use, references "myuser" throughout. modules/default.nix only imports public infrastructure modules. .gitignore blocks secrets, planning, and sops config from ever being committed.
  </action>
  <verify>grep -E "dangirsh|personal-agent-runtime|claw-swap\.com|dangirsh\.org|automaton" flake.nix && echo "FAIL: private refs found" || echo "OK: no private refs"; grep -E "home-assistant|spacebot|automaton|openclaw|matrix|repos" modules/default.nix && echo "FAIL: private modules in default.nix" || echo "OK: no private modules"; grep "nixosModules" flake.nix && echo "OK: nixosModules exported" || echo "FAIL: nixosModules missing"; grep "\.planning" .gitignore && echo "OK" || echo "FAIL"</verify>
  <done>No private refs in flake.nix; no private modules in default.nix; nixosModules.default exported; .gitignore includes .planning/ and secrets/</done>
</task>

<task type="auto">
  <name>Task 4: Scrub private service details from homepage.nix and verify nix flake check</name>
  <files>modules/homepage.nix</files>
  <action>
    In modules/homepage.nix:
    - Remove the entire "Applications" services section that references: claw-swap (href claw-swap.com, localhost:3000), dangirsh.org (href dangirsh.org), Parts Tools (localhost:8080), Parts Agent (localhost:3001), Spacebot (port 19898).
    - Remove the entire "Home" section (Home Assistant at port 8123).
    - Remove the entire "OpenClaw" section (openclaw-mark, openclaw-lou, openclaw-alexia, openclaw-ari with personal domains).
    - Keep the "Infrastructure" section intact: Prometheus, Syncthing, Restic B2 — these are the generic patterns.
    - Replace the openmeteo widget timezone "Europe/Berlin" with a comment: # set timezone to your locale.
    - Add a comment in the services list: "# Add your services here — see private overlay for examples."
    - Keep docker socket widget and settings (title, theme, color, headerStyle, statusStyle) — generic.

    After homepage.nix is clean, verify the full flake still evaluates:
    Run: cd /data/projects/neurosys && git add -A && nix flake check 2>&1 | head -60

    If nix flake check fails due to removed inputs being referenced in remaining modules (e.g., openclaw.nix, matrix.nix still exist on disk and may be imported indirectly), investigate and resolve. The modules still exist as files but are no longer imported via default.nix — they should not cause eval failures since Nix only evaluates imported paths.

    Also check: modules/networking.nix references internalOnlyPorts with port numbers for private services (spacebot 19898, mautrix 29317/29318/29328, openclaw 18789-18792). Keep these as-is or replace with comments. Decision: replace with a single comment block: "# Add your private service ports here as examples:" followed by two generic placeholder entries. Keep the generic ones (8082 homepage, 8384 syncthing, 9090 prometheus, 9091 secret-proxy, 9100 node-exporter, 8123 HA, 6052 ESPHome).

    Since HA is a private module now, also remove 8123 and 6052 from internalOnlyPorts in networking.nix (they'll be added by the private overlay when those modules are imported).

    Result: homepage.nix shows only generic infrastructure services. networking.nix internalOnlyPorts contains only public-module ports. nix flake check passes.
  </action>
  <verify>cd /data/projects/neurosys && nix flake check 2>&1 | tail -20</verify>
  <done>nix flake check exits 0. No dangirsh.org, claw-swap.com, openclaw, or spacebot references in homepage.nix. networking.nix internalOnlyPorts contains only ports from imported public modules.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] grep -r "dangirsh\|161\.97\|135\.125\|dan@worldcoin\|dan\.girshovich\|Dan Girshovich\|personal-agent-runtime\|claw-swap\.com\|openclaw.*dangirsh" /data/projects/neurosys/modules /data/projects/neurosys/home /data/projects/neurosys/hosts /data/projects/neurosys/flake.nix returns zero results
- [ ] grep "nixosModules" /data/projects/neurosys/flake.nix returns a hit
- [ ] grep "\.planning" /data/projects/neurosys/.gitignore returns a hit
- [ ] grep "secrets/" /data/projects/neurosys/.gitignore returns a hit
- [ ] nix flake check passes in /data/projects/neurosys
- [ ] modules/default.nix does not import home-assistant, spacebot, automaton, openclaw, matrix, or repos
</verification>

<success_criteria>
- All four tasks completed
- No personal identifiers (IPs, SSH keys, real name, username, private repo URLs) in any tracked file
- nixosModules.default exported from flake.nix for private overlay consumption
- .gitignore excludes .planning/, secrets/, .sops.yaml
- modules/default.nix imports only public infrastructure modules
- nix flake check passes
</success_criteria>

<output>
After completion, create .planning/phases/37-open-source-prep-audit-for-privacy-security-split-private-overlay-write-principles-first-lean-readme/37-01-SUMMARY.md
</output>

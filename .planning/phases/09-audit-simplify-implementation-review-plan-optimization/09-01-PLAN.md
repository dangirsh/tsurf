---
phase: 09-audit-simplify-implementation-review-plan-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/networking.nix
  - modules/users.nix
  - modules/secrets.nix
  - secrets/acfs.yaml
autonomous: true

must_haves:
  truths:
    - "SSH is only accessible via Tailscale — port 22 is not open on the public interface"
    - "Root cannot SSH into the server under any circumstances"
    - "User accounts cannot be modified outside of NixOS config"
    - "Only wheel group members can execute sudo"
    - "Dead code (example_secret, root SSH keys) is removed"
    - "nix flake check passes after all changes"
  artifacts:
    - path: "modules/networking.nix"
      provides: "Tailscale-only SSH access with openFirewall=false"
      contains: "openFirewall = false"
    - path: "modules/users.nix"
      provides: "User hardening with mutableUsers=false and execWheelOnly"
      contains: "mutableUsers = false"
    - path: "modules/secrets.nix"
      provides: "Clean secrets declarations with no dead entries"
  key_links:
    - from: "modules/networking.nix"
      to: "services.openssh"
      via: "openFirewall = false prevents port 22 on public interface"
      pattern: "openFirewall = false"
    - from: "modules/networking.nix"
      to: "networking.firewall.trustedInterfaces"
      via: "tailscale0 trust allows SSH over Tailscale"
      pattern: "trustedInterfaces.*tailscale0"
---

<objective>
Harden security posture and remove dead code from all committed NixOS modules.

Purpose: Implement the three security fixes identified in research (SSH-to-Tailscale-only, root SSH removal, user hardening) and remove dead code (example_secret). These changes reduce the attack surface and clean up deployment artifacts that are no longer needed. Phase 2.1 items (mutableUsers, execWheelOnly) are absorbed here.

Output: Updated modules/networking.nix, modules/users.nix, modules/secrets.nix, secrets/acfs.yaml — all passing nix flake check.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-audit-simplify-implementation-review-plan-optimization/09-RESEARCH.md
@modules/networking.nix
@modules/users.nix
@modules/secrets.nix
</context>

<tasks>

<task type="auto">
  <name>Task 1: SSH-to-Tailscale-only + root SSH elimination</name>
  <files>modules/networking.nix, modules/users.nix</files>
  <action>
In modules/networking.nix:
1. Remove `22` from `networking.firewall.allowedTCPPorts` — change `[ 22 80 443 22000 ]` to `[ 80 443 22000 ]`
2. Add `openFirewall = false;` to `services.openssh` block (prevents NixOS from auto-opening port 22)
3. Change `PermitRootLogin = "prohibit-password"` to `PermitRootLogin = "no"`
4. Update the comment on PermitRootLogin: remove "key-only root for initial deploy recovery" — replace with "root SSH fully disabled; emergency access via Contabo VNC"
5. Update @decision annotations at top of file:
   - NET-01: change to "key-only SSH via Tailscale only, no root login"
   - NET-04: change to "ports 80, 443, 22000 on public interface; SSH via Tailscale only"

In modules/users.nix:
6. Remove the entire `users.users.root.openssh.authorizedKeys.keys` block (lines 17-21 including comment). Deployment is complete; dangirsh SSH + sudo verified working.
7. Remove the comment above it ("Root SSH access: keep during initial deployment for recovery. Remove after confirming dangirsh SSH + sudo works (Plan 02).")

Safety note: SSH will still work over Tailscale because `trustedInterfaces = [ "tailscale0" ]` already trusts all Tailscale traffic. Contabo VNC console is the emergency fallback.
  </action>
  <verify>
Run `nix flake check` in the repo root. Verify no port 22 appears in networking.nix. Verify no `users.users.root.openssh` appears in users.nix. Verify `openFirewall = false` is present in networking.nix. Verify `PermitRootLogin = "no"` is present.
  </verify>
  <done>
Port 22 removed from public firewall, openFirewall=false set, PermitRootLogin="no", root authorized_keys removed. SSH only accessible via Tailscale interface.
  </done>
</task>

<task type="auto">
  <name>Task 2: User hardening + dead code removal</name>
  <files>modules/users.nix, secrets/acfs.yaml</files>
  <action>
In modules/users.nix:
1. Add `users.mutableUsers = false;` — prevents passwd/useradd changes outside Nix config. Place it before the `users.users.dangirsh` block.
2. Add `security.sudo.execWheelOnly = true;` — only wheel group members can use sudo. Place it after the existing `security.sudo.wheelNeedsPassword = false;` line.
3. Update the @decision annotation at top: change to "SYS-01: dangirsh with sudo (wheel) + docker group; mutableUsers=false, execWheelOnly=true"

In secrets/acfs.yaml:
4. Remove the `example_secret` key from the encrypted file. This requires:
   - Run `sops secrets/acfs.yaml` to open the editor (or use `sops --set '["example_secret"] null'` followed by manual removal)
   - Actually, since the file has placeholder values and we just need to remove one key: use `sops -d secrets/acfs.yaml` to decrypt, remove the `example_secret` line, then `sops -e -i secrets/acfs.yaml` to re-encrypt in place
   - If sops is not available or decryption fails (key not on this machine), note the finding and skip — the executor can check sops availability first. If sops is available: `sops -d secrets/acfs.yaml > /tmp/acfs-plain.yaml`, edit out `example_secret`, then `sops -e /tmp/acfs-plain.yaml > secrets/acfs.yaml` (use project tmp/ not /tmp/).
   - Important: Do NOT modify any other secret values. Only remove the `example_secret` key.

Note: If sops/age private key is not available on this machine, skip the secrets/acfs.yaml change and document it as a TODO. The key removal is low-priority cleanup.
  </action>
  <verify>
Run `nix flake check`. Verify `users.mutableUsers = false` appears in users.nix. Verify `security.sudo.execWheelOnly = true` appears in users.nix. Check that `example_secret` is not referenced in any .nix module (grep for it). If sops edit succeeded, verify secrets/acfs.yaml no longer contains `example_secret`.
  </verify>
  <done>
mutableUsers=false and execWheelOnly=true are set. example_secret removed from secrets file (or documented as TODO if sops unavailable). nix flake check passes.
  </done>
</task>

</tasks>

<verification>
1. `nix flake check` passes with all changes
2. `grep -r 'port.*22\|allowedTCPPorts.*22' modules/` returns no matches (port 22 gone from firewall)
3. `grep 'openFirewall = false' modules/networking.nix` matches
4. `grep 'PermitRootLogin.*no' modules/networking.nix` matches
5. `grep 'mutableUsers = false' modules/users.nix` matches
6. `grep 'execWheelOnly = true' modules/users.nix` matches
7. `grep 'root.*authorizedKeys' modules/users.nix` returns no matches
8. `grep 'example_secret' modules/` returns no matches
</verification>

<success_criteria>
- SSH is Tailscale-only: port 22 removed from public firewall + openFirewall=false
- Root SSH eliminated: PermitRootLogin="no" + root authorized_keys removed
- User hardening applied: mutableUsers=false + execWheelOnly=true
- Dead code removed: example_secret cleaned up
- nix flake check passes
- All @decision annotations updated to reflect new security posture
</success_criteria>

<output>
After completion, create `.planning/phases/09-audit-simplify-implementation-review-plan-optimization/09-01-SUMMARY.md`
</output>

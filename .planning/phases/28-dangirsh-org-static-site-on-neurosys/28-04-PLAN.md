---
phase: 28-dangirsh-org-static-site-on-neurosys
plan: 04
type: execute
wave: 3
depends_on: ["28-03"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true

must_haves:
  truths:
    - "Content update workflow documented: edit site -> push -> nix flake update dangirsh-site -> deploy"
    - "NearlyFreeSpeech can be decommissioned (no traffic flowing to NFS)"
    - "ROADMAP.md updated with Phase 28 as complete"
    - "STATE.md updated with Phase 28 completion and key decisions"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 28 marked complete with success criteria"
    - path: ".planning/STATE.md"
      provides: "Phase 28 decisions and completion recorded"
  key_links: []
---

<objective>
Post-cutover cleanup: document the content update workflow, verify NFS can be decommissioned, update planning docs.

Purpose: With dangirsh.org live on OVH, this plan ties up loose ends: documents the manual content update workflow (per WORKFLOW-01), verifies no traffic flows to NFS, and updates project tracking.

Output: Update workflow documented, NFS decommission confirmed safe, planning docs current.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-dangirsh-org-static-site-on-neurosys/28-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document content update workflow and verify NFS decommission safety</name>
  <files></files>
  <action>
**Step 1: Verify NFS is no longer receiving traffic**

Check that DNS no longer points to NFS:
```bash
dig +short dangirsh.org
# Must return 135.125.196.143, NOT 208.94.117.137
```

Wait at least 48 hours after DNS cutover before decommissioning NFS (some DNS resolvers cache aggressively).

**Step 2: Document content update workflow**

The manual update workflow (per user decision WORKFLOW-01):

1. Edit site content in the `dangirsh-site` repo (local or on GitHub)
2. Push to GitHub: `cd /data/projects/dangirsh-site && git push`
3. Update the flake input in neurosys: `cd /data/projects/neurosys && nix flake update dangirsh-site`
4. Deploy: `./scripts/deploy.sh --node ovh`

This rebuilds the site derivation from the updated source and updates the nginx root symlink atomically.

The workflow summary should be included in the plan summary (not a separate doc — per simplicity conventions, avoid creating docs for simple workflows).

**Step 3: NFS decommission guidance**

After confirming no traffic reaches NFS (48h+ after DNS cutover):
1. Cancel or downgrade NearlyFreeSpeech hosting account
2. Remove old deploy scripts from dangirsh-site repo if desired (`deploy-*.sh`, `publi.sh`, `setup_ssh_key.sh`, `DEPLOY_*.md`)
3. These can be removed in a follow-up cleanup commit — not blocking

**Step 4: Verify the full content update cycle works**

Test the workflow end-to-end:
1. Make a trivial change to a file in `dangirsh-site` (e.g., update a date or add a comment to a template)
2. Push to GitHub
3. `nix flake update dangirsh-site` in neurosys
4. `./scripts/deploy.sh --node ovh`
5. Verify the change is live: `curl https://dangirsh.org` shows the updated content

This validates the WORKFLOW-01 manual update cycle.
  </action>
  <verify>
- DNS confirms OVH IP (not NFS)
- Content update workflow tested end-to-end
- No traffic to NFS IP
  </verify>
  <done>
Content update workflow validated. NFS decommission confirmed safe. Manual workflow: edit -> push -> nix flake update -> deploy.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md and STATE.md</name>
  <files>
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <action>
**Update ROADMAP.md:**

1. Update the Phase 28 entry in the phases list:
   ```
   - [x] **Phase 28: dangirsh.org Static Site on Neurosys** — Hakyll flake, nginx unified proxy (dangirsh.org + claw-swap.com), ACME TLS, DNS cutover from NFS, Docker Caddy removed
   ```

2. Add Phase 28 details section with goal, depends_on, success criteria, and plans list.

3. Update the Progress table with Phase 28 status.

**Update STATE.md:**

1. Update "Current Position" to reflect Phase 28 completion.

2. Add Phase 28 decisions to the Decisions section:
   - [28-01]: HOST-01: dangirsh.org served from OVH (production), not Contabo
   - [28-01]: Hakyll site built as flake packages.x86_64-linux.default, consumed as neurosys flake input
   - [28-02]: SECURITY-01: nginx as unified reverse proxy, Docker Caddy removed from claw-swap
   - [28-02]: WEB-01-06: @decision annotations in nginx.nix (ACME, static serving, www redirect, caching)
   - [28-02]: ACME state persisted in /var/lib/acme via impermanence
   - [28-03]: DNS A records for dangirsh.org + www cutover to OVH IP 135.125.196.143
   - [28-04]: WORKFLOW-01: Manual update cycle: edit -> push -> nix flake update -> deploy

3. Add Phase 28 to Completed Phases.

4. Update Roadmap Evolution with Phase 28 execution note.
  </action>
  <verify>
- `grep 'Phase 28' .planning/ROADMAP.md` shows complete status
- `grep 'Phase 28' .planning/STATE.md` shows in Completed Phases
- `grep 'dangirsh.org' .planning/STATE.md` shows key decisions
  </verify>
  <done>
ROADMAP.md and STATE.md updated with Phase 28 completion, decisions, and workflow documentation.
  </done>
</task>

</tasks>

<verification>
1. DNS confirms dangirsh.org points to OVH (135.125.196.143)
2. Content update workflow tested: edit -> push -> nix flake update -> deploy -> verified live
3. ROADMAP.md shows Phase 28 complete with all 4 plans
4. STATE.md records all Phase 28 decisions
5. NFS decommission is safe (no traffic)
</verification>

<success_criteria>
Phase 28 is fully complete. dangirsh.org is live on OVH with automated ACME TLS, nginx unified proxy, and a documented manual content update workflow. NearlyFreeSpeech can be decommissioned. Planning docs are current.
</success_criteria>

<output>
After completion, create `.planning/phases/28-dangirsh-org-static-site-on-neurosys/28-04-SUMMARY.md`
</output>

---
phase: 10-parts-consolidation-migrate-parts-from-standalone-vps-to-acfs-via-neurosys
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "A deployment command from neurosys builds and switches the NixOS config including parts containers/services"
    - "The deployment flow is tested end-to-end: deploy script runs, NixOS switches, containers come up"
    - "Parts repo defines its own deployable components via nixosModules.default -- neurosys does not hardcode parts internals"
  artifacts: []
  key_links:
    - from: "scripts/deploy.sh"
      to: "acfs NixOS system"
      via: "nixos-rebuild switch"
      pattern: "Deploy SUCCESS"
---

<objective>
Run the deploy script end-to-end against the acfs server and verify the full deployment pipeline works.

Purpose: Phase success criteria #4 requires the deployment flow to be tested end-to-end. This plan runs the actual deploy, verifies containers come up, and confirms the pipeline works from start to finish.

Output: Verified deployment, confirmed container health, user sign-off.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-parts-consolidation-migrate-parts-from-standalone-vps-to-acfs-via-neurosys/10-01-SUMMARY.md
@scripts/deploy.sh
@flake.nix
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run deploy script and capture output</name>
  <files></files>
  <action>
Run the deploy script in local-push mode from the neurosys repo root:

```bash
cd /data/projects/neurosys
./scripts/deploy.sh --mode local
```

This will:
1. Update the parts flake input to latest main
2. Build the NixOS system closure locally
3. Copy the closure to the acfs server via SSH
4. Activate the new system on the server
5. Poll Docker containers for health
6. Print the deploy summary

Capture the full output. If the deploy fails:
- Check the error message for common issues (SSH connectivity, build errors, missing inputs)
- If SSH fails: verify `ssh root@acfs` works manually
- If build fails: check `nix flake check` output for evaluation errors
- If containers fail: check `ssh root@acfs "docker ps -a"` and `ssh root@acfs "journalctl -u docker-parts-tools.service --no-pager -n 50"` for startup errors

If deploy succeeds, note the output showing:
- Parts revision deployed
- Container statuses
- Deploy duration
  </action>
  <verify>
1. Deploy script exits 0 (success)
2. Output contains "Deploy SUCCESS"
3. Output shows all 5 containers running (parts-tools, parts-agent, claw-swap-db, claw-swap-app, claw-swap-caddy)
4. Parts revision is displayed in output
  </verify>
  <done>Deploy script ran end-to-end successfully, all containers are running on the acfs server.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify deployment and sign off</name>
  <what-built>
Full deploy pipeline: scripts/deploy.sh updated the parts flake input, built the NixOS system, deployed to acfs, and verified all containers are running.
  </what-built>
  <how-to-verify>
1. Review the deploy output from Task 1 — confirm all containers show as running
2. Optionally SSH into acfs and spot-check:
   - `ssh root@acfs "docker ps"` — all 5 containers running
   - `ssh root@acfs "nixos-rebuild list-generations | tail -3"` — new generation exists
3. Confirm the deploy script behavior is satisfactory (output format, timing, error handling)
4. Check `git diff flake.lock` to see the parts input update — commit when ready

If anything looks wrong, describe the issue. Otherwise type "approved".
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Deploy script ran and exited 0
2. All 5 containers are running on acfs
3. NixOS generation was created by the deploy
4. User approved the deployment
</verification>

<success_criteria>
- End-to-end deploy completes without errors
- All containers (parts-tools, parts-agent, claw-swap-db, claw-swap-app, claw-swap-caddy) are running
- User verifies and approves the pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/10-parts-consolidation-migrate-parts-from-standalone-vps-to-acfs-via-neurosys/10-02-SUMMARY.md`
</output>

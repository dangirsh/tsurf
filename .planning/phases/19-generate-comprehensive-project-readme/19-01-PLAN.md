---
phase: 19-generate-comprehensive-project-readme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true

must_haves:
  truths:
    - "README.md exists at repo root with all sections from success criteria"
    - "A first-time deployer can follow the quick-start to deploy the system"
    - "Every module and service in modules/ and home/ is documented"
    - "Design decisions and accepted risks are in table format"
    - "Operations section has concrete commands for deploy, backup, monitoring, and secrets"
    - "Content is skimmable -- bullets, tables, headers dominate over prose"
  artifacts:
    - path: "README.md"
      provides: "Comprehensive project documentation"
      contains: "neurosys"
  key_links:
    - from: "README.md"
      to: "modules/*.nix"
      via: "Module table enumerating all 13 modules"
      pattern: "base\\.nix|boot\\.nix|networking\\.nix|users\\.nix|secrets\\.nix|docker\\.nix|monitoring\\.nix|syncthing\\.nix|home-assistant\\.nix|homepage\\.nix|agent-compute\\.nix|repos\\.nix|restic\\.nix"
    - from: "README.md"
      to: "scripts/deploy.sh"
      via: "Deploy quick-start section with concrete commands"
      pattern: "deploy\\.sh"
    - from: "README.md"
      to: "docs/recovery-runbook.md"
      via: "Backup & restore operations section references runbook"
      pattern: "recovery-runbook"
---

<objective>
Write a comprehensive, skimmable README.md for the neurosys repository covering all modules, services, security model, deployment, operations, design decisions, and accepted risks.

Purpose: Someone who needs to understand and operate this system quickly can use this README as their single entry point. A first-time deployer can follow the quick-start section to deploy the system without prior knowledge.

Output: README.md at repo root
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-generate-comprehensive-project-readme/19-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md from module source files</name>
  <files>README.md</files>
  <action>
Create README.md at the repo root. CRITICAL: Every fact must come from the actual `.nix` source files, NOT from planning docs (which are stale in parts). Read each module before writing its section.

Structure (in this exact order):

1. **Header + one-line description**: "neurosys — NixOS configuration for the neurosys server"

2. **Overview** (3-5 bullets): What this is, what it manages, target audience. Mention: Contabo VPS, flakes + home-manager + sops-nix, Docker services, agent compute, monitoring, backups.

3. **Infrastructure** (bullet list): VPS specs (18 vCPU AMD EPYC, 96GB RAM, 350GB NVMe), static IP 161.97.74.121, hostname neurosys, NixOS 25.11, timezone Europe/Berlin.

4. **Architecture** (brief): Flake inputs table (7 inputs: nixpkgs, home-manager, sops-nix, disko, parts, claw-swap, llm-agents). Module organization (modules/, home/, packages/).

5. **Modules table**: All 13 NixOS modules from modules/ — one row per module with module name, short description, and key config highlights. Read each module file to verify facts. Include: base, boot, networking, users, secrets, docker, monitoring, syncthing, home-assistant, homepage, agent-compute, repos, restic.

6. **Home-Manager Modules table**: All 7 home-manager modules from home/ — default, bash, git, ssh, direnv, cass, agent-config.

7. **Custom Packages table**: zmx (v0.3.0, terminal session persistence), cass (v0.1.64, agent session indexer). Note: pre-built binaries with SHA256 verification.

8. **Services & Ports table**: All services with their port, access level (public/Tailscale-only/localhost-only). Extract from networking.nix (allowedTCPPorts, internalOnlyPorts) and individual modules. Public: 80, 443, 22000. Tailscale-only: 22 (SSH), 8082 (homepage), 8123 (Home Assistant), 6052 (ESPHome), 9100 (node_exporter). Localhost-only: 9090 (Prometheus), 8384 (Syncthing GUI).

9. **Security Model** (bullet list, NOT prose): nftables default-deny firewall, SSH Tailscale-only with build-time assertion, fail2ban progressive banning, sops-nix age-encrypted secrets (from SSH host key), bubblewrap sandbox for agents, kernel sysctl hardening (dmesg, kptr, BPF, redirects), metadata endpoint blocked. Note Docker --iptables=false.

10. **Deployment Quick-Start**: Prerequisites (Nix with flakes, admin age key, Tailscale access to neurosys). Commands:
    - Validate: `nix flake check`
    - Build: `nixos-rebuild build --flake .#neurosys`
    - Deploy: `./scripts/deploy.sh` (default target: root@neurosys via Tailscale MagicDNS)
    - Deploy flags: `--mode local|remote`, `--target <host>`, `--skip-update`
    Brief note about deploy.sh two-mode operation (local build + remote switch, or remote rebuild) and locking.

11. **Operations** (subsections with concrete commands):
    - **Deploy**: deploy.sh synopsis, modes, health polling, rollback
    - **Backup & Restore**: restic to B2 (S3 API), daily timer, retention (7/5/12), blanket "/" with --one-file-system + exclusions, .nobackup sentinel. Mention pg_dumpall pre-hook. Link to docs/recovery-runbook.md for full restore procedure. RTO < 2hr, RPO 24hr.
    - **Monitoring**: Prometheus on localhost:9090 (15s scrape, 90d retention), 7 alert rules (list them), node_exporter textfile collector for restic staleness. Homepage dashboard at :8082 for quick status.
    - **Secrets**: sops-nix with age keys (admin + host), 7 secrets + 1 template in secrets/neurosys.yaml. How to add a secret: add to secrets.nix, encrypt with `sops secrets/neurosys.yaml`, deploy. Note: age key derived from SSH host key.
    - **Agent Compute**: agent-spawn (bubblewrap sandbox by default, --no-sandbox opt-out), zmx sessions, claude/codex CLIs, Podman rootless, agent.slice cgroup limits, audit logging.

12. **Design Decisions table**: ID, Decision, Rationale. Source from @decision annotations across all modules. Use the comprehensive list from 19-RESEARCH.md but VERIFY each against actual source code. Key decisions: NET-01 through RESTIC-05, HA-01/02, HP-01, SANDBOX-11-01, etc.

13. **Accepted Risks table**: ID, Risk, Mitigation. From CLAUDE.md accepted risks section: SEC3, SEC5, SEC6, SEC9, SEC11, plus sandbox design choices (cross-project read, no network sandbox).

14. **Project Structure**: File tree matching the one in CLAUDE.md (this is authoritative). Include flake.nix, flake.lock, hosts/, modules/, home/, packages/, scripts/, secrets/, docs/.

15. **Footer**: Link to CLAUDE.md for development/agent conventions. Link to docs/recovery-runbook.md for disaster recovery.

IMPORTANT things to NOT include (removed/never-existed):
- Ollama, Zsh, Atuin, rustup, Bun, pnpm, Go, Python, Neovim — all dropped
- Grafana, ntfy, Alertmanager — removed in quick-005
- tmux — replaced by zmx
- CrowdSec — not yet implemented (Phase 15)
- Hostname "acfs" — renamed to "neurosys"

Format rules:
- Tables for enumerations (modules, services, decisions, risks, ports)
- Bullet lists for features, prerequisites, steps
- Code blocks for commands
- Prose limited to 1-2 sentence introductions per section
- Use `<details>` for lengthy subsections only if needed to keep main flow scannable
- Target ~300-500 lines of Markdown
  </action>
  <verify>
Verify README.md exists:
```
test -f README.md && echo "EXISTS" || echo "MISSING"
```

Verify key sections present:
```
grep -c "^##" README.md  # Should have 10+ level-2 headers
grep -q "deploy.sh" README.md && echo "deploy: OK"
grep -q "restic" README.md && echo "backup: OK"
grep -q "Prometheus" README.md && echo "monitoring: OK"
grep -q "sops-nix" README.md && echo "secrets: OK"
grep -q "bubblewrap" README.md && echo "sandbox: OK"
```

Verify NO stale content:
```
grep -qi "ollama\|grafana\|alertmanager\|ntfy\|acfs\|tmux" README.md && echo "STALE CONTENT FOUND" || echo "No stale content"
```
  </verify>
  <done>
README.md exists at repo root with all 15 sections. Every module in modules/ and home/ has a table entry. Services & ports table covers all ports. Design decisions and accepted risks are in table format. Deploy quick-start has concrete commands. No stale content (Ollama, Grafana, ntfy, Alertmanager, tmux, acfs).
  </done>
</task>

<task type="auto">
  <name>Task 2: Cross-validate README claims against source code</name>
  <files>README.md</files>
  <action>
Systematically verify every factual claim in the README against the actual .nix source files. This is the quality gate that prevents stale/incorrect information.

Verification checklist:

1. **Flake inputs**: Read flake.nix, confirm exactly 7 inputs and their names match README table
2. **Module count**: `ls modules/*.nix | wc -l` should match "13 modules" claim (subtract default.nix if it's just an import hub)
3. **Home module count**: `ls home/*.nix | wc -l` should match README claim
4. **Port numbers**: For each port in the Services & Ports table:
   - Verify allowedTCPPorts in networking.nix
   - Verify internalOnlyPorts list in networking.nix
   - Verify individual module port configs
5. **Package versions**: Verify zmx version in packages/zmx.nix, cass version in packages/cass.nix
6. **Secret count**: Count secrets in modules/secrets.nix, confirm "7 secrets + 1 template"
7. **Alert rules**: Count alert rules in modules/monitoring.nix, confirm "7 alert rules" and names match
8. **Restic retention**: Verify 7/5/12 in modules/restic.nix
9. **Deploy flags**: Read scripts/deploy.sh, confirm --mode, --target, --skip-update exist
10. **VPS specs**: Verify static IP in hosts/neurosys/default.nix

Fix any discrepancies found. If a fact in the README doesn't match the source code, update the README to match the source code (source code is truth).

After fixes, run the stale content check again:
```
grep -Eiq "ollama|grafana|alertmanager|ntfy|atuin|zsh|acfs[^-]|tmux[^/]" README.md && echo "STALE" || echo "CLEAN"
```
  </action>
  <verify>
All 10 verification checks pass. No discrepancies between README claims and source code.
  </verify>
  <done>
Every factual claim in README.md has been verified against source code. Port numbers, module counts, package versions, secret counts, alert rule counts, retention policy, deploy flags, and VPS specs all match the actual .nix files.
  </done>
</task>

</tasks>

<verification>
1. README.md exists at repo root: `test -f README.md`
2. All sections present: 10+ level-2 headers covering overview, architecture, modules, security, deployment, operations, decisions, risks
3. No stale content: `grep -Eiq "ollama|grafana|alertmanager|ntfy|atuin|zsh|acfs[^-]|tmux[^/]" README.md` returns no matches
4. Factual accuracy: All module counts, port numbers, package versions, and secret counts verified against source code
5. Skimmable format: Tables for modules, ports, decisions, risks; bullets for features; code blocks for commands
6. Deploy quick-start: Concrete commands with prerequisites listed
</verification>

<success_criteria>
- README.md exists at repo root with complete, accurate content covering all 13 NixOS modules and 7 home-manager modules
- First-time deployer can follow the quick-start section (prerequisites + commands + verification)
- Operations section has concrete commands for deploy, backup, monitoring, secrets, and agent compute
- Design decisions (15+) and accepted risks (6+) are in table format
- Content is skimmable: tables, bullets, and headers dominate; no prose paragraphs over 2 sentences
- Zero stale content: no mentions of dropped features (Ollama, Grafana, ntfy, Alertmanager, tmux, Zsh, Atuin, acfs)
</success_criteria>

<output>
After completion, create `.planning/phases/19-generate-comprehensive-project-readme/19-01-SUMMARY.md`
</output>

# modules/nginx.nix
# Private overlay placeholder.
# Move nginx virtual hosts, certificates, and domain-specific routing to your private overlay.
{ ... }: {
  # Intentionally empty in the public template.
}

# Bootstrap Scripts

Provider-specific bootstrap scripts for initial NixOS installation.
Not part of the core deploy pipeline.

- `bootstrap-ovh.sh` — OVH VPS bootstrap via rescue mode + nixos-anywhere
